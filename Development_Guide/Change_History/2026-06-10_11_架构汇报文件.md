# 架构汇报文件记录

## 目的

形成一份可直接用于组内架构汇报和后续开发宣贯的统一材料。

## 新增内容

- 新增 `08_架构设计与后续开发汇报.md`。
- 说明架构调整原因、分层职责、数据流和任务模型。
- 汇总此前 BASEPRI、中断、no data、数据撕裂、任务栈、周期漂移、
  状态重复、告警覆盖、半帧发送和时间单位问题。
- 给出每类问题的架构规避方式。
- 增加临时任务准入规则、登记项和禁止事项。
- 列出架构优势、当前限制、团队强制规则和下一阶段计划。
- 将汇报中的总体架构改为普通查看器可见的纯文本框图。
- Mermaid 可编辑源图单独保存为
  `08_架构设计与后续开发汇报_总体架构图.mmd`。

## 代码影响

本次只增加开发和汇报文档，不修改源代码、工程配置和固件行为。
