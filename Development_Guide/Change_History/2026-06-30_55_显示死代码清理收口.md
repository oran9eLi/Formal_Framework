# 2026-06-30 55 显示死代码清理收口

## 背景

整体死代码复查时确认产品显示路径已经统一到 LVGL，旧 `display_log`、`display_pages`、`display_text` 不再承担有效运行职责，继续保留会误导后续 5G/RemoteID 页面接入。

## 本次收口

1. 删除旧显示日志和旧页面适配文件：
   - `Display/Inc/display_log.h`
   - `Display/Src/display_log.c`
   - `Display/Inc/display_pages.h`
   - `Display/Src/display_pages.c`
   - `Display/Inc/display_text.h`
   - `Display/Src/display_text.c`
2. 从 Keil 工程 source list 移除上述旧源文件。
3. 本机消息日志统一读取 Business `app_message_log` 和 Framework 本地消息日志版本号，LVGL 不再依赖显示层私有日志缓存。
4. 删除空实现兼容入口 `Display_PollTouch()`、`Display_HandleTouch()`、`Display_ToggleDataSource()`、`Display_ShowBootCode()` 以及 Business 侧空触摸轮询 shim。
5. 消息日志页面文本保持 ASCII 短码，避免中文字模缺失时出现方框。

## 栈与资源

- 未扩大任何任务栈。
- 未新增动态内存。
- 删除旧路径后固件尺寸下降。

## 验证

Keil Rebuild All：

```text
Program Size: Code=262104 RO-data=91068 RW-data=1320 ZI-data=126632
".\Objects\formal_framework.axf" - 0 Error(s), 0 Warning(s).
Build Time Elapsed: 00:01:10
```
