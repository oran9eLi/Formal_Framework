# 2026-06-30 54 LoRa远端显示链路收口

## 变更内容

- 修正启动初期丢包率显示：LoRa Driver 与 MAVLink RX 丢包率均在 32 帧样本后开始显示。
- 修正远端数据周期性消失：远端数据过期阈值改为 15 s，覆盖 5 s 状态帧和 10 s 日志/告警帧周期。
- 加固选中远端后的 stream request：`COMMAND_LONG` 在收到 `COMMAND_ACK` 前有限重试。
- 新增湿度远端同步：BME280 湿度通过 `NAMED_VALUE_INT/ENVHUM` 外发并写入远端环境快照。
- 修正远端消息日志不同步：REMOTE_LOG TUNNEL 解包后缓存日志条目，Display 通过 App API 读取本机或远端结构化日志。
- 修正日志字体方框：LVGL 消息日志行改为 ASCII 短码显示，避免中文字模缺字。
- 修正电机未接入显示绿色：电机状态灯优先使用第二路电池/电机供电状态裁决。
- 加固 IMU 掉线恢复：MPU6050 driver-level reinit 前执行 BSP I2C bus recover。

## 验证结果

- Keil MDK-ARM Rebuild：`0 Error(s), 0 Warning(s)`。
- Program Size：`Code=263936 RO-data=91068 RW-data=1404 ZI-data=126708`。
- 本次未扩大任务栈，`PX4LITE_STACK_COMM` 保持 `384U` words。
