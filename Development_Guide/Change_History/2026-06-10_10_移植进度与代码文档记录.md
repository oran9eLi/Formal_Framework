# 移植进度与代码文档记录

## 修改原因

当前工程已经完成多轮架构迁移，但“真实功能、占位接口、后续步骤”和
“代码文档完成度”分散在多个文件中，容易让开发人员把能够编译误认为
功能已经完成。

## 本次记录

- 新增 `07_移植进度与功能清单.md`。
- 明确架构迁移第一阶段已经完成。
- 明确当前真实运行闭环以 GNSS 为主。
- 列出已经使用、基础完成、骨架、占位和预留功能。
- 记录阶段 2 至阶段 6 的开发任务和验收条件。
- 增加每个新模块必须填写的十项移植信息。
- 已为 52 个自研 `.c/.h` 文件添加文件用途头。
- 厂商 HAL、CMSIS、FreeRTOS 和第三方代码不重复添加注释。

## 当前代码文档状态

| 项目 | 状态 |
|---|---|
| 自研源文件文件头 | 已完成 |
| 厂商和第三方文件 | 排除，不修改 |
| 自研公开 API 函数用途 | 已完成，96 个声明 |
| 自研逻辑函数用途 | 已完成，128 个定义 |
| 复杂接口参数、返回值、调用上下文 | 持续细化 |
| 模板代码文档检查 | 待模板定版后执行 |

## 验证状态

- 架构第一阶段此前已经 ARMCC 5.06 全量编译验证：
  `0 Error / 0 Warning`。
- 本次文件头和 Markdown 记录完成后已再次执行全量编译：
  `0 Error / 0 Warning`。
- Program Size：
  `Code=33768, RO-data=728, RW-data=236, ZI-data=46476`。
- 本次构建日志：
  `MDK-ARM/build_documentation_step1.log`。
- 函数级注释完成后，需要再次全量编译并更新 `Release/Latest`。

## 后续接续点

下一步在实现各真实模块时同步补充复杂接口的参数、返回值、调用上下文、
并发方式和执行预算，不再允许新增无用途说明的自研函数。
