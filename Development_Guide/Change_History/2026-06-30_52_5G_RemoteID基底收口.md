# 2026-06-30 5G/RemoteID 基底收口

## 改动

- 接入第二电池/外设独立供电链路：新增 ADC2/PA4 BSP、`sensor_power2`、`Px4Lite_Battery2Read()`、Battery2 topic、App Environment 字段、Display 电机电池字段和 MAVLink `BAT2STAT`。
- 接入远端扩展数据面：新增 `px4lite_remote_tunnel`、`px4lite_local_msglog`，通过 MAVLink `TUNNEL` 中继远端告警表和消息日志，通过 `NAMED_VALUE_INT` 中继第二电池和电机目标输出。
- 补 LoRa 本机存在性接口：Driver 暴露 `Lora_E22_IsPresent()`，Platform 暴露 `Px4Lite_LoRaIsPresent()`，继续区分本机硬件事实和对端 RX 新鲜度。
- 本地显示日志同步写入 Framework 固定环形消息日志，供 `REMOTE_LOG` 增量发送；未引入动态内存。
- Keil 工程 source list 已加入新增 BSP、Sensor、Framework 源文件。

## 边界

- 未迁入 LL 旧 `display_pages/display_gfx/display_text` 手绘页面框架；F1 继续以 LVGL 为唯一显示页面框架。
- 未新增普通任务，未扩大 `PX4LITE_STACK_COMM` 或其他任务栈。
- 远端告警表缓存限制为每节点 5 行，匹配当前显示行数，避免 17 节点静态 RAM 继续膨胀。
- REMOTE 数据逐字段编码，不发送 C struct 内存。

## 验证

- Keil Rebuild All：`.\Objects\formal_framework.axf - 0 Error(s), 0 Warning(s)`。
- Program Size：`Code=260648 RO-data=90668 RW-data=1180 ZI-data=124636`。
