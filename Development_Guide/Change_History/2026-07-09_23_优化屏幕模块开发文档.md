# 2026-07-09_23 优化屏幕模块开发文档

## 修改原因

`18_屏幕模块开发文档.md` 仍以旧点阵渲染开发流程为主，未充分覆盖当前工程已启用的 LVGL 后端、本地/远端按钮、远端显示同构、中文字体子集和当前 DisplayTask 栈配置。继续沿用旧文档容易让后续开发者在新增页面、变量或触摸事件时改错层级。

## 修改内容

| 文件 | 改动 |
|---|---|
| `Development_Guide/18_屏幕模块开发文档.md` | 重写为当前 LVGL 后端下的二次开发手册 |

主要更新：

- 明确当前 `DISPLAY_USE_LVGL_BACKEND = 1U`，新增正式页面优先走 `display_lvgl.c`。
- 补充分层边界：BSP、SSD1963/GT911 驱动、Display 协调层、LVGL 后端、旧点阵后端、Business 调度。
- 更新数据链路为 App 快照 / App Display Model -> Display HMI 变量 -> LVGL 控件 -> flush。
- 增加当前页面表、本地/远端隐藏页入口说明。
- 增加新增显示变量、LVGL 页面、普通数值控件、触摸按钮、中文文字、本地/远端显示字段的开发步骤。
- 更新 DisplayTask 参数，`BUSINESS_DISPLAY_TASK_STACK_WORDS` 改为当前 1280。
- 增加调度、性能、自检恢复、合入门禁和常见问题排查建议。

## 行为变化

本次只修改开发文档，不改变代码行为。

文档口径变化：

- 从“旧点阵页面开发手册”为主，调整为“当前 LVGL 后端 Display 模块开发手册”。
- 旧点阵渲染被明确为硬件调试、回退和迁移对照用途。
- 本地/远端显示、消息日志、告警表统一走 App Display Model 的约束写入开发规则。

## 验证

- 已读取更新后的 `18_屏幕模块开发文档.md`，确认 UTF-8 中文显示正常。
- 已确认文档中包含当前关键口径：LVGL 后端、`display_lvgl.c`、本地/远端、`BUSINESS_DISPLAY_TASK_STACK_WORDS = 1280`、`Change_History` 门禁。
- 本次不涉及编译或烧录。

## 后续

若后续继续调整 LVGL 页面结构、远端显示协议或字体子集，应同步更新 `18_屏幕模块开发文档.md`，不要只写入单次 `Change_History`。

