# 阶段3普通传感器接入关闭

## 背景

当前产品基线中的 GNSS、IMU、气压计和电源数据接入已经形成完整链路，可支撑显示、通信、日志和健康状态使用。后续如果替换正式器件或新增普通传感器，应按统一接入手册执行，不再把阶段 3 作为未完成阶段持续打开。

## 变更

- 在 `05_完成度与后续清单.md` 中将“阶段 3：普通传感器接入”标记为“已关闭”。
- 明确阶段 3 关闭范围：当前 GNSS、IMU、Baro、Power 普通传感器基线已闭环。
- 明确后续新增或替换普通传感器时，继续遵守单一 `sensor` 任务、强类型快照、状态单写者和恢复边界。

## 验证

- 本次仅修改文档完成度口径和变更记录，未修改固件源码、Keil 工程或外设配置。
- 未运行 Keil build verification；原因是本次不涉及可执行代码或工程配置变化。
