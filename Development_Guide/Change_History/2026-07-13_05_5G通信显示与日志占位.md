# 2026-07-13 05 5G通信显示与日志占位

## 本次改动

- 自检页和各业务页系统栏新增 `5G通信` 状态项，复用现有 `PX4LITE_MODULE_5G` 模块状态。
- 原 `通信` 自检项显示名称调整为 `LoRa通信`。
- 消息日志中原 `通信正常/通信断开` 文案调整为 `LoRa正常/LoRa断开`。
- 消息日志枚举追加 `5G正常/5G断开`，保持追加式编号，不重排已有消息 ID。
- 自检汇总栏和告警页中通信离线原因按来源区分：
  - LoRa：`通信 / LoRa离线`
  - RemoteID：`通信 / RemoteID离线`
  - 5G：`通信 / 5G通信离线`

## 边界说明

- 本次不新增 5G 在线/离线硬件判定条件。
- 5G 灯色、日志和告警只消费现有 `PX4LITE_MODULE_5G` 状态；后续 5G 驱动或服务接入后再补实际状态更新逻辑。

## 验证

- 已完成源码差异检查和枚举同步检查。
- 本机未找到 Keil `UV4.exe` 命令入口，本轮未能执行 Keil Rebuild All。

## 追加修正

- 自检汇总栏和告警页在 5G 底层在线判定接入前，按现有 `PX4LITE_MODULE_5G` 模块状态追加显示层占位离线告警，避免 5G 红灯但告警列表为空。
- 消息日志中 `LoRa正常/断开`、`RemoteID正常/断开`、`5G正常/断开` 恢复为与其他日志一致的完整同行文本，不在中间拆列留空。
- 启动阶段首次得到自检状态时立即写入自检日志，避免等待 LoRa/RemoteID/5G 等通信状态稳定期间自检日志区域为空。
- 启动批量日志改为固定延时输出：模块状态变化只更新待输出快照，不再重置启动等待时间，避免消息日志被持续变化的通信/传感器状态卡住。
