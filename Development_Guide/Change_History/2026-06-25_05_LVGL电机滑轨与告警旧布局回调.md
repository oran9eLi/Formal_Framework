# 2026-06-25_05_LVGL电机滑轨与告警旧布局回调

## 变更摘要

- 电机页中间油门区回到旧点阵图的坐标体系，四路滑轨按旧的 270/355/440/525 横向位置摆放。
- 去掉每路滑轨旁边的 0/100 刻度文字，缩短滑轨高度并减小滑块，避免滑轨被其它对象遮挡。
- 急停按钮放回中间油门卡片底部的旧坐标区域，触发逻辑仍走 `Display_RequestMotorEmergencyStop()`。
- 告警页撤掉底部消息日志，改回旧点阵图的“告警汇总 + 三列表格 + 五行告警”排列方式。

## 验证

- `display_lvgl.c`、`display_lvgl_font_zh.c`、`display.c`、`display_pages.c` 已通过 ARMCC5 单文件编译检查。
