# 完整告警表远端同步 (Part 1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** REMOTE 模式下，显示端的告警页显示目标设备的**完整活动告警表**（多行），而非现在的最高 1 行。

**Architecture:** 发送端把本机告警 topic 的 active 行用 TUNNEL(payload_type=0x8001) 打包，on-change 立即发 + 慢速保活；接收端解包整表替换写入 RemoteTelemetry；`App_GetDisplayAlarm` 的 REMOTE 分支把远端行填满 `App_AlarmSnapshot_t.records[]`。打包/解包是一对纯函数，桌面单测覆盖。两套 UI 经统一接口零改动。

**Tech Stack:** C99、STM32F407、MAVLink v2(common dialect, `mavlink_msg_tunnel.h`)、桌面 gcc 单测(MSYS2 gcc 16)。

设计依据：`Development_Guide/LY/20_告警表与消息日志远端同步实现设计.md` Part 1（§3–§9）。

## Global Constraints

- 无动态分配；固定容量缓冲；按 comm / biz 周期构建。
- 打包载体只用 `TUNNEL`，禁止 `NAMED_VALUE_INT` 逐行。
- 载荷一律**小端**；payload_type `0x8001` = 完整告警表。
- 告警行 7 字节：`source_id`(1)+`fault_code`(2)+`severity`(1)+`active`(1)+`age_s`(2)；表头 2 字节：`ver`(1)+`active_count`(1)。
- 只发 `active != 0` 的行；行数上限 `PX4LITE_MODULE_COUNT`。
- 断链：App 层判 `STALE` 保留旧表，不清零、不回退本机。
- 桌面单测用 `Tests/Unit/stubs/` 下的 `stm32f4xx.h`/`stm32f4xx_hal.h` 桩 shadow CMSIS/HAL（已存在）。
- 每个 commit 信息以 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` 结尾。

---

### Task 1: 告警表 TUNNEL 编解码（纯函数 + 桌面单测）

**Files:**
- Create: `Framework/Inc/px4lite_remote_tunnel.h`
- Create: `Framework/Src/px4lite_remote_tunnel.c`
- Test: `Tests/Unit/test_remote_tunnel_alarm.c`

**Interfaces:**
- Consumes: `Px4Lite_AlarmRecord_t`、`Px4Lite_Result_t`、`Px4Lite_AlarmSeverity_t`、`PX4LITE_MODULE_COUNT`（均在 `Framework/Inc/px4lite_types.h`）。
- Produces:
  - `uint16_t Px4Lite_PackAlarmTable(const Px4Lite_AlarmRecord_t *records, uint8_t record_count, uint8_t ver, uint32_t now_ms, uint8_t *out, uint16_t out_cap)` — 打包 active 行，返回 payload 字节数，错误返回 0。
  - `Px4Lite_Result_t Px4Lite_UnpackAlarmTable(const uint8_t *payload, uint16_t len, uint32_t now_ms, Px4Lite_AlarmRecord_t *out_records, uint8_t out_cap, uint8_t *out_count, uint8_t *out_ver)`。
  - `uint32_t Px4Lite_AlarmTableSignature(const Px4Lite_AlarmRecord_t *records, uint8_t record_count)` — active 行内容签名(不含 age)。
  - 宏：`PX4LITE_TUNNEL_PT_ALARM_TABLE`、`PX4LITE_TUNNEL_ALARM_ROW_BYTES`、`PX4LITE_TUNNEL_ALARM_HEADER_BYTES`、`PX4LITE_TUNNEL_ALARM_MAX_BYTES`。

- [ ] **Step 1: 写头文件**

Create `Framework/Inc/px4lite_remote_tunnel.h`:

```c
/**
 * @file px4lite_remote_tunnel.h
 * @brief 远端显示 TUNNEL 打包载荷的编解码契约(告警表)。布局见 LY/20 §4。
 */
#ifndef PX4LITE_REMOTE_TUNNEL_H
#define PX4LITE_REMOTE_TUNNEL_H

#include <stdint.h>
#include "px4lite_types.h"

#define PX4LITE_TUNNEL_PT_ALARM_TABLE 0x8001U /**< TUNNEL payload_type：完整告警表。 */

#define PX4LITE_TUNNEL_ALARM_HEADER_BYTES 2U  /**< ver(1)+active_count(1)。 */
#define PX4LITE_TUNNEL_ALARM_ROW_BYTES    7U  /**< source_id(1)+fault_code(2)+severity(1)+active(1)+age_s(2)。 */
#define PX4LITE_TUNNEL_ALARM_MAX_ROWS     ((uint8_t)PX4LITE_MODULE_COUNT)
#define PX4LITE_TUNNEL_ALARM_MAX_BYTES \
  (PX4LITE_TUNNEL_ALARM_HEADER_BYTES + (uint16_t)PX4LITE_TUNNEL_ALARM_ROW_BYTES * PX4LITE_TUNNEL_ALARM_MAX_ROWS)

/**
 * @brief 把 active 告警行打包为告警表载荷(小端)。
 * @param records 本机告警记录数组。 @param record_count 数组长度。
 * @param ver 表内容版本，写入表头。 @param now_ms 用于计算 age_s。
 * @param out 输出缓冲。 @param out_cap 输出容量。
 * @return payload 字节数；参数非法或容量不足表头返回 0。容量不足时截断到能容纳的整行。
 */
uint16_t Px4Lite_PackAlarmTable(const Px4Lite_AlarmRecord_t *records, uint8_t record_count,
                                uint8_t ver, uint32_t now_ms, uint8_t *out, uint16_t out_cap);

/**
 * @brief 解包告警表载荷到记录数组。age_s 还原为 raised_ms = now - age_s*1000(近似)。
 * @return PX4LITE_OK 成功；PX4LITE_INVALID_PARAM 空指针/长度不足/行越界。
 */
Px4Lite_Result_t Px4Lite_UnpackAlarmTable(const uint8_t *payload, uint16_t len, uint32_t now_ms,
                                          Px4Lite_AlarmRecord_t *out_records, uint8_t out_cap,
                                          uint8_t *out_count, uint8_t *out_ver);

/**
 * @brief active 行内容签名(source_id+fault_code+severity+active，不含 age_s)，用于 on-change 检测。
 */
uint32_t Px4Lite_AlarmTableSignature(const Px4Lite_AlarmRecord_t *records, uint8_t record_count);

#endif /* PX4LITE_REMOTE_TUNNEL_H */
```

- [ ] **Step 2: 写失败测试**

Create `Tests/Unit/test_remote_tunnel_alarm.c`:

```c
/**
 * @file test_remote_tunnel_alarm.c
 * @brief 验证告警表 TUNNEL 打包/解包 round-trip 与签名。
 */
#include <stdint.h>
#include <stdio.h>
#include <string.h>

#include "px4lite_remote_tunnel.h"

static int g_fail;

static void ExpectU32(const char *name, uint32_t a, uint32_t e)
{
  if (a != e) { printf("FAIL %s actual=%lu expected=%lu\n", name, (unsigned long)a, (unsigned long)e); g_fail++; }
}

static Px4Lite_AlarmRecord_t MakeRow(uint16_t src, uint16_t code, uint8_t sev, uint8_t active, uint32_t raised_ms)
{
  Px4Lite_AlarmRecord_t r;
  memset(&r, 0, sizeof(r));
  r.source_id = src; r.fault_code = code; r.severity = (Px4Lite_AlarmSeverity_t)sev;
  r.active = active; r.raised_ms = raised_ms;
  return r;
}

static void TestRoundTripTwoActiveRows(void)
{
  Px4Lite_AlarmRecord_t in[3];
  uint8_t buf[PX4LITE_TUNNEL_ALARM_MAX_BYTES];
  Px4Lite_AlarmRecord_t out[PX4LITE_TUNNEL_ALARM_MAX_ROWS];
  uint8_t out_count = 0xFFU, out_ver = 0U;
  uint16_t len;
  uint32_t now = 10000U;

  in[0] = MakeRow(3U, 0x0102U, 2U, 1U, 7000U); /* age=3s */
  in[1] = MakeRow(5U, 0x0000U, 0U, 0U, 0U);    /* inactive: 不发 */
  in[2] = MakeRow(9U, 0x00ABU, 1U, 1U, 9500U); /* age=0s(向下取整) */

  len = Px4Lite_PackAlarmTable(in, 3U, 7U, now, buf, sizeof(buf));
  ExpectU32("len two rows", len, PX4LITE_TUNNEL_ALARM_HEADER_BYTES + 2U * PX4LITE_TUNNEL_ALARM_ROW_BYTES);
  ExpectU32("header ver", buf[0], 7U);
  ExpectU32("header count", buf[1], 2U);

  ExpectU32("unpack ok", Px4Lite_UnpackAlarmTable(buf, len, now, out, PX4LITE_TUNNEL_ALARM_MAX_ROWS, &out_count, &out_ver), PX4LITE_OK);
  ExpectU32("out count", out_count, 2U);
  ExpectU32("out ver", out_ver, 7U);
  ExpectU32("row0 src", out[0].source_id, 3U);
  ExpectU32("row0 code", out[0].fault_code, 0x0102U);
  ExpectU32("row0 sev", (uint32_t)out[0].severity, 2U);
  ExpectU32("row0 active", out[0].active, 1U);
  ExpectU32("row0 raised", out[0].raised_ms, now - 3000U);
  ExpectU32("row1 src", out[1].source_id, 9U);
  ExpectU32("row1 code", out[1].fault_code, 0x00ABU);
}

static void TestEmptyTable(void)
{
  Px4Lite_AlarmRecord_t in[1];
  uint8_t buf[PX4LITE_TUNNEL_ALARM_MAX_BYTES];
  Px4Lite_AlarmRecord_t out[PX4LITE_TUNNEL_ALARM_MAX_ROWS];
  uint8_t out_count = 0xFFU, out_ver = 0xFFU;
  uint16_t len;

  in[0] = MakeRow(1U, 1U, 1U, 0U, 0U); /* inactive */
  len = Px4Lite_PackAlarmTable(in, 1U, 0U, 1000U, buf, sizeof(buf));
  ExpectU32("empty len", len, PX4LITE_TUNNEL_ALARM_HEADER_BYTES);
  ExpectU32("empty header count", buf[1], 0U);
  ExpectU32("empty unpack ok", Px4Lite_UnpackAlarmTable(buf, len, 1000U, out, PX4LITE_TUNNEL_ALARM_MAX_ROWS, &out_count, &out_ver), PX4LITE_OK);
  ExpectU32("empty out count", out_count, 0U);
}

static void TestSignatureIgnoresAge(void)
{
  Px4Lite_AlarmRecord_t a = MakeRow(3U, 0x0102U, 2U, 1U, 1000U);
  Px4Lite_AlarmRecord_t b = MakeRow(3U, 0x0102U, 2U, 1U, 8000U); /* 不同 raised_ms */
  Px4Lite_AlarmRecord_t c = MakeRow(3U, 0x0103U, 2U, 1U, 1000U); /* 不同 code */
  ExpectU32("sig age-invariant", Px4Lite_AlarmTableSignature(&a, 1U), Px4Lite_AlarmTableSignature(&b, 1U));
  if (Px4Lite_AlarmTableSignature(&a, 1U) == Px4Lite_AlarmTableSignature(&c, 1U)) { printf("FAIL sig should differ on code\n"); g_fail++; }
}

static void TestUnpackRejectsShort(void)
{
  uint8_t buf[3] = {1U, 5U, 0U}; /* 声称 5 行但 len 不够 */
  Px4Lite_AlarmRecord_t out[PX4LITE_TUNNEL_ALARM_MAX_ROWS];
  uint8_t cnt = 0U, ver = 0U;
  ExpectU32("short rejected", Px4Lite_UnpackAlarmTable(buf, 3U, 0U, out, PX4LITE_TUNNEL_ALARM_MAX_ROWS, &cnt, &ver), PX4LITE_INVALID_PARAM);
}

int main(void)
{
  TestRoundTripTwoActiveRows();
  TestEmptyTable();
  TestSignatureIgnoresAge();
  TestUnpackRejectsShort();
  if (g_fail == 0) { printf("PASS test_remote_tunnel_alarm\n"); return 0; }
  printf("FAIL test_remote_tunnel_alarm fails=%d\n", g_fail);
  return 1;
}
```

- [ ] **Step 3: 运行测试确认失败**

Run（在仓库根，Bash/MSYS2 gcc）:
```bash
gcc -std=c99 -Wall -Wextra -I Tests/Unit/stubs -I Framework/Inc \
  Tests/Unit/test_remote_tunnel_alarm.c Framework/Src/px4lite_remote_tunnel.c -o /tmp/t_alarm.exe
```
Expected: 编译失败（`px4lite_remote_tunnel.c` 尚不存在 / 链接缺函数）。

- [ ] **Step 4: 写实现**

Create `Framework/Src/px4lite_remote_tunnel.c`:

```c
/**
 * @file px4lite_remote_tunnel.c
 * @brief 远端显示 TUNNEL 载荷编解码(告警表)。纯函数，无全局状态、无动态分配。
 */
#include "px4lite_remote_tunnel.h"
#include <string.h>

static void Tun_PutU16(uint8_t *p, uint16_t v) { p[0] = (uint8_t)(v & 0xFFU); p[1] = (uint8_t)((v >> 8) & 0xFFU); }
static uint16_t Tun_GetU16(const uint8_t *p) { return (uint16_t)((uint16_t)p[0] | ((uint16_t)p[1] << 8)); }

uint16_t Px4Lite_PackAlarmTable(const Px4Lite_AlarmRecord_t *records, uint8_t record_count,
                                uint8_t ver, uint32_t now_ms, uint8_t *out, uint16_t out_cap)
{
  uint16_t off = PX4LITE_TUNNEL_ALARM_HEADER_BYTES;
  uint8_t n = 0U;
  uint8_t i;

  if ((records == 0) || (out == 0) || (out_cap < PX4LITE_TUNNEL_ALARM_HEADER_BYTES)) { return 0U; }

  for (i = 0U; i < record_count; ++i) {
    uint8_t *row;
    uint32_t age_s;

    if (records[i].active == 0U) { continue; }
    if ((uint16_t)(off + PX4LITE_TUNNEL_ALARM_ROW_BYTES) > out_cap) { break; }

    row    = &out[off];
    row[0] = (uint8_t)(records[i].source_id & 0xFFU);
    Tun_PutU16(&row[1], records[i].fault_code);
    row[3] = (uint8_t)records[i].severity;
    row[4] = records[i].active;
    age_s  = (now_ms - records[i].raised_ms) / 1000U;
    if (age_s > 0xFFFFU) { age_s = 0xFFFFU; }
    Tun_PutU16(&row[5], (uint16_t)age_s);

    off = (uint16_t)(off + PX4LITE_TUNNEL_ALARM_ROW_BYTES);
    n++;
  }

  out[0] = ver;
  out[1] = n;
  return off;
}

Px4Lite_Result_t Px4Lite_UnpackAlarmTable(const uint8_t *payload, uint16_t len, uint32_t now_ms,
                                          Px4Lite_AlarmRecord_t *out_records, uint8_t out_cap,
                                          uint8_t *out_count, uint8_t *out_ver)
{
  uint16_t off = PX4LITE_TUNNEL_ALARM_HEADER_BYTES;
  uint8_t stored = 0U;
  uint8_t n;
  uint8_t i;

  if ((payload == 0) || (out_records == 0) || (out_count == 0) || (out_ver == 0)) { return PX4LITE_INVALID_PARAM; }
  if (len < PX4LITE_TUNNEL_ALARM_HEADER_BYTES) { return PX4LITE_INVALID_PARAM; }

  *out_ver = payload[0];
  n        = payload[1];

  for (i = 0U; i < n; ++i) {
    const uint8_t *row;
    if ((uint16_t)(off + PX4LITE_TUNNEL_ALARM_ROW_BYTES) > len) { return PX4LITE_INVALID_PARAM; }
    if (stored < out_cap) {
      row = &payload[off];
      memset(&out_records[stored], 0, sizeof(out_records[stored]));
      out_records[stored].source_id  = (uint16_t)row[0];
      out_records[stored].fault_code = Tun_GetU16(&row[1]);
      out_records[stored].severity   = (Px4Lite_AlarmSeverity_t)row[3];
      out_records[stored].active     = row[4];
      out_records[stored].raised_ms  = now_ms - (uint32_t)Tun_GetU16(&row[5]) * 1000U;
      out_records[stored].updated_ms = now_ms;
      stored++;
    }
    off = (uint16_t)(off + PX4LITE_TUNNEL_ALARM_ROW_BYTES);
  }

  *out_count = stored;
  return PX4LITE_OK;
}

uint32_t Px4Lite_AlarmTableSignature(const Px4Lite_AlarmRecord_t *records, uint8_t record_count)
{
  uint32_t h = 2166136261U; /* FNV-1a 32 */
  uint8_t i;

  if (records == 0) { return 0U; }
  for (i = 0U; i < record_count; ++i) {
    if (records[i].active == 0U) { continue; }
    h = (h ^ (uint32_t)(records[i].source_id & 0xFFU)) * 16777619U;
    h = (h ^ (uint32_t)(records[i].fault_code & 0xFFU)) * 16777619U;
    h = (h ^ (uint32_t)((records[i].fault_code >> 8) & 0xFFU)) * 16777619U;
    h = (h ^ (uint32_t)records[i].severity) * 16777619U;
    h = (h ^ (uint32_t)records[i].active) * 16777619U;
  }
  return h;
}
```

- [ ] **Step 5: 运行测试确认通过**

Run:
```bash
gcc -std=c99 -Wall -Wextra -I Tests/Unit/stubs -I Framework/Inc \
  Tests/Unit/test_remote_tunnel_alarm.c Framework/Src/px4lite_remote_tunnel.c -o /tmp/t_alarm.exe && /tmp/t_alarm.exe
```
Expected: `PASS test_remote_tunnel_alarm`，无 error/warning。

- [ ] **Step 6: 提交**

```bash
git add Framework/Inc/px4lite_remote_tunnel.h Framework/Src/px4lite_remote_tunnel.c Tests/Unit/test_remote_tunnel_alarm.c
git commit -m "feat: 新增告警表TUNNEL编解码纯函数与单测"
```

---

### Task 2: RemoteTelemetry 告警行字段 + App_GetDisplayAlarm REMOTE

**Files:**
- Modify: `Framework/Inc/px4lite_remote_telemetry.h`（在快照结构体加字段）
- Modify: `Business/Src/app_display_model.c`（`App_GetDisplayAlarm` REMOTE 分支填满 records）
- Test: `Tests/Unit/test_app_display_alarm_remote.c`

**Interfaces:**
- Consumes: `Px4Lite_RemoteTelemetrySnapshot_t`、`Px4Lite_RemoteTelemetryMutable()`、`Px4Lite_RemoteTelemetryCopySnapshot()`、`App_AlarmSnapshot_t`、`App_AlarmRecord_t`。
- Produces（供 Task 3 RX 写入）：快照新增 `alarm_records[PX4LITE_MODULE_COUNT]`、`alarm_table_count`、`alarm_table_ver`、`alarm_table_update_ms`。

- [ ] **Step 1: 给快照加字段**

Modify `Framework/Inc/px4lite_remote_telemetry.h`，在 `Px4Lite_RemoteTelemetrySnapshot_t` 内 `alarm_active_mask` 字段之后插入：

```c
  Px4Lite_AlarmRecord_t alarm_records[PX4LITE_MODULE_COUNT]; /**< 远端活动告警行(完整告警表)。 */
  uint8_t alarm_table_count;    /**< 远端活动告警行数。 */
  uint8_t alarm_table_ver;      /**< 远端告警表内容版本(TUNNEL ver)。 */
  uint16_t reserved_alarm_tbl;  /**< 保留字段，保持结构体对齐。 */
  uint32_t alarm_table_update_ms; /**< 告警表最近更新时间，单位 ms。 */
```

（`Px4Lite_AlarmRecord_t` 已由 `px4lite_types.h` 提供，本头已 `#include "px4lite_types.h"`。）

- [ ] **Step 2: 写失败测试**

Create `Tests/Unit/test_app_display_alarm_remote.c`。它桩掉 app_display_model.c 的依赖，直接构造远端快照后调 `App_GetDisplayAlarm`：

```c
/**
 * @file test_app_display_alarm_remote.c
 * @brief REMOTE 模式 App_GetDisplayAlarm 返回完整远端告警行。
 */
#include <stdint.h>
#include <stdio.h>
#include <string.h>

#include "app_display_model.h"
#include "px4lite_remote_telemetry.h"

static int g_fail;
static Px4Lite_RemoteMode_t s_mode;

Px4Lite_RemoteMode_t App_GetRemoteDisplayMode(void) { return s_mode; }
Px4Lite_Result_t App_CopyAlarm(App_AlarmSnapshot_t *out, uint32_t now_ms) { (void)out; (void)now_ms; return PX4LITE_NOT_READY; }
/* 其余 App_Copy*/App_GetModuleStatus/App_GetCommStats 仅在别的 getter 用到，这里桩空。 */
Px4Lite_Result_t App_CopyNavigation(App_NavigationSnapshot_t *o, uint32_t n) { (void)o;(void)n; return PX4LITE_NOT_READY; }
Px4Lite_Result_t App_CopyEnvironment(App_EnvironmentSnapshot_t *o, uint32_t n) { (void)o;(void)n; return PX4LITE_NOT_READY; }
Px4Lite_Result_t App_CopySystem(App_SystemSnapshot_t *o, uint32_t n) { (void)o;(void)n; return PX4LITE_NOT_READY; }
Px4Lite_Result_t App_CopyMotor(App_MotorSnapshot_t *o, uint32_t n) { (void)o;(void)n; return PX4LITE_NOT_READY; }
Px4Lite_Result_t App_CopyDateTime(App_DateTimeSnapshot_t *o, uint32_t n) { (void)o;(void)n; return PX4LITE_NOT_READY; }
Px4Lite_Result_t App_GetModuleStatus(Px4Lite_ModuleId_t m, Px4Lite_ModuleStatus_t *o) { (void)m;(void)o; return PX4LITE_NOT_READY; }
void App_GetCommStats(Px4Lite_CommDebugInfo_t *o) { if (o) memset(o, 0, sizeof(*o)); }

static void ExpectU32(const char *name, uint32_t a, uint32_t e)
{ if (a != e) { printf("FAIL %s actual=%lu expected=%lu\n", name, (unsigned long)a, (unsigned long)e); g_fail++; } }

static void TestRemoteReturnsFullTable(void)
{
  Px4Lite_RemoteTelemetrySnapshot_t *snap;
  App_AlarmSnapshot_t out;
  uint32_t now = 5000U;

  Px4Lite_RemoteTelemetryInit(now);
  (void)Px4Lite_RemoteTelemetrySetMode(PX4LITE_REMOTE_MODE_REMOTE, now);
  s_mode = PX4LITE_REMOTE_MODE_REMOTE;

  snap = Px4Lite_RemoteTelemetryMutable();
  memset(snap->alarm_records, 0, sizeof(snap->alarm_records));
  snap->alarm_records[0].source_id = 3U; snap->alarm_records[0].fault_code = 0x10U; snap->alarm_records[0].severity = (Px4Lite_AlarmSeverity_t)2U; snap->alarm_records[0].active = 1U;
  snap->alarm_records[1].source_id = 7U; snap->alarm_records[1].fault_code = 0x22U; snap->alarm_records[1].severity = (Px4Lite_AlarmSeverity_t)1U; snap->alarm_records[1].active = 1U;
  snap->alarm_table_count = 2U;
  snap->highest_fault_code = 0x10U; snap->highest_source_id = 3U; snap->highest_severity = 2U;
  Px4Lite_RemoteTelemetryCommit(2U, 1U, PX4LITE_REMOTE_VALID_ALARM, now);

  memset(&out, 0, sizeof(out));
  ExpectU32("remote alarm ok", App_GetDisplayAlarm(&out, now), PX4LITE_OK);
  ExpectU32("active_count", out.active_count, 2U);
  ExpectU32("rec0 src", out.records[0].source_id, 3U);
  ExpectU32("rec1 src", out.records[1].source_id, 7U);
  ExpectU32("rec1 code", out.records[1].fault_code, 0x22U);
}

int main(void)
{
  TestRemoteReturnsFullTable();
  if (g_fail == 0) { printf("PASS test_app_display_alarm_remote\n"); return 0; }
  printf("FAIL test_app_display_alarm_remote fails=%d\n", g_fail);
  return 1;
}
```

- [ ] **Step 3: 运行确认失败**

Run:
```bash
gcc -std=c99 -Wall -Wextra -I Tests/Unit/stubs -I Business/Inc -I Framework/Inc \
  Tests/Unit/test_app_display_alarm_remote.c Business/Src/app_display_model.c \
  Framework/Src/px4lite_remote_telemetry.c -o /tmp/t_alarm2.exe && /tmp/t_alarm2.exe
```
Expected: FAIL —— `rec1 src` 等失败（现 REMOTE 只填 highest 1 行）。

- [ ] **Step 4: 改 App_GetDisplayAlarm REMOTE 分支**

在 `Business/Src/app_display_model.c` 的 `App_GetDisplayAlarm` REMOTE 实现里，取得远端快照后，用远端行填满 `out->records`（替换"只填 records[0]=highest"）：

```c
  /* REMOTE：用远端完整告警行填满 records；无行时回退到 highest 摘要。 */
  {
    uint8_t i;
    uint8_t n = remote.alarm_table_count;
    if (n > (uint8_t)PX4LITE_MODULE_COUNT) { n = (uint8_t)PX4LITE_MODULE_COUNT; }
    out->active_count = n;
    for (i = 0U; i < n; ++i) {
      out->records[i].source_id  = remote.alarm_records[i].source_id;
      out->records[i].fault_code = remote.alarm_records[i].fault_code;
      out->records[i].severity   = (uint8_t)remote.alarm_records[i].severity;
      out->records[i].active     = remote.alarm_records[i].active;
      out->records[i].updated_ms = now_ms;
      out->records[i].detail     = remote.alarm_records[i].detail;
    }
    out->highest_fault_code = remote.highest_fault_code;
    out->highest_source_id  = remote.highest_source_id;
    out->highest_severity   = remote.highest_severity;
  }
```

> 说明：`App_AlarmRecord_t` 字段（`source_id`/`fault_code`/`severity`/`active`/`updated_ms`/`detail`）见 `app_data_api.h`；若现有 REMOTE 分支已有变量名 `remote`/`out`/`now_ms`，沿用之，仅替换记录填充段。保留原有 `fresh`/返回值逻辑不动。

- [ ] **Step 5: 运行确认通过**

Run（同 Step 3 命令）。Expected: `PASS test_app_display_alarm_remote`。

- [ ] **Step 6: 提交**

```bash
git add Framework/Inc/px4lite_remote_telemetry.h Business/Src/app_display_model.c Tests/Unit/test_app_display_alarm_remote.c
git commit -m "feat: 远端快照保存完整告警行并由显示告警接口返回"
```

---

### Task 3: RX —— TUNNEL(0x8001) 解包写入远端快照

**Files:**
- Modify: `Framework/Src/px4lite_mavlink_rx.c`（新增 `MavlinkRx_HandleTunnel` + dispatch case）
- Test: `Tests/Unit/test_mavlink_rx_alarm_tunnel.c`

**Interfaces:**
- Consumes: `Px4Lite_PackAlarmTable`（构造测试帧用）、`Px4Lite_UnpackAlarmTable`、`mavlink_msg_tunnel_*`、`Px4Lite_RemoteTelemetryMutable/Commit`。
- Produces: RX 后远端快照 `alarm_records/alarm_table_count/alarm_table_ver` 被写入。

- [ ] **Step 1: 写 handler 与 dispatch**

在 `Framework/Src/px4lite_mavlink_rx.c` 顶部 include 处加 `#include "px4lite_remote_tunnel.h"`。新增 handler（放在其它 `MavlinkRx_Handle*` 旁）：

```c
static Px4Lite_Result_t MavlinkRx_HandleTunnel(const mavlink_message_t *msg, uint32_t now_ms)
{
  mavlink_tunnel_t tun;
  Px4Lite_RemoteTelemetrySnapshot_t *snap;
  uint8_t count = 0U;
  uint8_t ver = 0U;

  mavlink_msg_tunnel_decode(msg, &tun);
  if (tun.payload_type != PX4LITE_TUNNEL_PT_ALARM_TABLE) { return PX4LITE_IDLE; }

  snap = Px4Lite_RemoteTelemetryMutable();
  if (Px4Lite_UnpackAlarmTable(tun.payload, tun.payload_length, now_ms,
                               snap->alarm_records, (uint8_t)PX4LITE_MODULE_COUNT,
                               &count, &ver) != PX4LITE_OK) {
    return PX4LITE_IO_ERROR;
  }
  snap->alarm_table_count     = count;
  snap->alarm_table_ver       = ver;
  snap->alarm_table_update_ms = now_ms;
  Px4Lite_RemoteTelemetryCommit(msg->sysid, msg->compid, PX4LITE_REMOTE_VALID_ALARM, now_ms);
  return PX4LITE_OK;
}
```

在 `Px4Lite_MavlinkRxHandleFrame` 的 `switch` 里加：

```c
    case MAVLINK_MSG_ID_TUNNEL:
      result = MavlinkRx_HandleTunnel(&msg, now_ms);
      break;
```

- [ ] **Step 2: 写失败测试**

Create `Tests/Unit/test_mavlink_rx_alarm_tunnel.c`：构造一个 TUNNEL 帧 → 序列化为 LoRa 帧 → `Px4Lite_MavlinkRxHandleFrame` → 检查远端快照。

```c
/**
 * @file test_mavlink_rx_alarm_tunnel.c
 * @brief RX 收到 TUNNEL(0x8001) 后写入远端告警行。
 */
#include <stdint.h>
#include <stdio.h>
#include <string.h>

#include "common/mavlink.h"
#include "px4lite_mavlink_rx.h"
#include "px4lite_remote_telemetry.h"
#include "px4lite_remote_tunnel.h"

static int g_fail;
static void ExpectU32(const char *n, uint32_t a, uint32_t e)
{ if (a != e) { printf("FAIL %s actual=%lu expected=%lu\n", n, (unsigned long)a, (unsigned long)e); g_fail++; } }

int main(void)
{
  uint32_t now = 8000U;
  Px4Lite_AlarmRecord_t rows[2];
  uint8_t payload[PX4LITE_TUNNEL_ALARM_MAX_BYTES];
  uint16_t plen;
  mavlink_message_t msg;
  uint8_t buf[300];
  uint16_t flen;
  Px4Lite_LoRaRxFrame_t frame;
  Px4Lite_RemoteTelemetrySnapshot_t out;

  Px4Lite_RemoteTelemetryInit(now);
  (void)Px4Lite_RemoteTelemetrySetMode(PX4LITE_REMOTE_MODE_REMOTE, now);
  Px4Lite_MavlinkRxInit(now);

  memset(rows, 0, sizeof(rows));
  rows[0].source_id = 4U; rows[0].fault_code = 0x55U; rows[0].severity = (Px4Lite_AlarmSeverity_t)2U; rows[0].active = 1U; rows[0].raised_ms = now - 2000U;
  rows[1].source_id = 6U; rows[1].fault_code = 0x66U; rows[1].severity = (Px4Lite_AlarmSeverity_t)1U; rows[1].active = 1U; rows[1].raised_ms = now - 1000U;
  plen = Px4Lite_PackAlarmTable(rows, 2U, 9U, now, payload, sizeof(payload));

  /* 远端 sysid 必须 != 本机，否则被自收过滤。 */
  (void)mavlink_msg_tunnel_pack_chan(2U, 1U, MAVLINK_COMM_0, &msg,
                                     PX4LITE_MAVLINK_SYSTEM_ID, 0U,
                                     PX4LITE_TUNNEL_PT_ALARM_TABLE, (uint8_t)plen, payload);
  flen = mavlink_msg_to_send_buffer(buf, &msg);

  memset(&frame, 0, sizeof(frame));
  frame.length = (uint16_t)flen;
  frame.system_id = 2U;
  memcpy(frame.data, buf, flen);

  ExpectU32("handle ok", Px4Lite_MavlinkRxHandleFrame(&frame, now), PX4LITE_OK);
  ExpectU32("copy ok", Px4Lite_RemoteTelemetryCopySnapshot(&out, now), PX4LITE_OK);
  ExpectU32("count", out.alarm_table_count, 2U);
  ExpectU32("ver", out.alarm_table_ver, 9U);
  ExpectU32("rec0 src", out.alarm_records[0].source_id, 4U);
  ExpectU32("rec1 code", out.alarm_records[1].fault_code, 0x66U);

  if (g_fail == 0) { printf("PASS test_mavlink_rx_alarm_tunnel\n"); return 0; }
  printf("FAIL test_mavlink_rx_alarm_tunnel fails=%d\n", g_fail);
  return 1;
}
```

> 注：`Px4Lite_LoRaRxFrame_t` 的字段名（`length`/`system_id`/`data`）需与 `px4lite_platform.h` 实际一致；执行时若字段名不同，按真实定义调整这三行赋值。`frame.data` 容量须 ≥ flen。

- [ ] **Step 3: 运行确认失败**

Run:
```bash
gcc -std=c99 -Wall -Wextra -I Tests/Unit/stubs -I Framework/Inc -I Third_Party/mavlink -I Third_Party/mavlink/common \
  Tests/Unit/test_mavlink_rx_alarm_tunnel.c Framework/Src/px4lite_mavlink_rx.c \
  Framework/Src/px4lite_remote_telemetry.c Framework/Src/px4lite_remote_tunnel.c -o /tmp/t_rx.exe && /tmp/t_rx.exe
```
Expected: 先因缺 dispatch（Step 1 未保存时）失败；Step 1 完成后应转 PASS。若初次因 `Px4Lite_LoRaRxFrame_t` 字段名不符编译失败，按真实字段名修正测试再跑。

- [ ] **Step 4: 运行确认通过**

Run（同上）。Expected: `PASS test_mavlink_rx_alarm_tunnel`。

- [ ] **Step 5: 提交**

```bash
git add Framework/Src/px4lite_mavlink_rx.c Tests/Unit/test_mavlink_rx_alarm_tunnel.c
git commit -m "feat: 接收端解包TUNNEL告警表写入远端快照"
```

---

### Task 4: TX —— 告警表槽(on-change + 保活)

**Files:**
- Modify: `Framework/Inc/px4lite_config.h`（新增 enable 宏 + 周期）
- Modify: `Framework/Src/px4lite_mavlink_tx.c`（新增 slot、发送函数、签名/版本/计时静态量、stats 计数）

**Interfaces:**
- Consumes: `Px4Lite_CopyAlarm`、`Px4Lite_IsFresh`、`Px4Lite_PackAlarmTable`、`Px4Lite_AlarmTableSignature`、`mavlink_msg_tunnel_pack_chan`、`MavTx_SendPrepared`、`MavTx_TimeReached`。
- Produces: TX 在告警 active 行内容变化时立即发 TUNNEL(0x8001)，否则每 `PX4LITE_MAVLINK_REMOTE_ALARM_PERIOD_MS` 保活一帧。

- [ ] **Step 1: 加配置宏**

在 `Framework/Inc/px4lite_config.h` 的 MAVLink 周期段加：

```c
#define PX4LITE_MAVLINK_ENABLE_REMOTE_ALARM    1U
#define PX4LITE_MAVLINK_REMOTE_ALARM_PERIOD_MS 2000U /**< 告警表保活周期；on-change 不受此限。 */
```

- [ ] **Step 2: 加 slot 枚举与静态量**

在 `px4lite_mavlink_tx.c`：枚举 `MavTx_Slot_t` 中 `MAV_TX_SLOT_STATUSTEXT` 之后、`MAV_TX_SLOT_COUNT` 之前插入 `MAV_TX_SLOT_REMOTE_ALARM,`。在静态量区加：

```c
static uint32_t s_next_remote_alarm_ms;
static uint32_t s_last_alarm_sig;
static uint8_t  s_alarm_ver;
```

`Px4Lite_MavlinkTxInit` 内初始化（与其它 `s_next_*` 一处）：`s_next_remote_alarm_ms = now_ms; s_last_alarm_sig = 0U; s_alarm_ver = 0U;`。
并在 `Px4Lite_MavlinkTxStats_t`（其定义头文件）加计数字段 `uint32_t remote_alarm_count;`（若 stats 结构在 `px4lite_mavlink_tx.h`，同步加）。

- [ ] **Step 3: 写发送函数**

在 `px4lite_mavlink_tx.c` 加（放在其它 `MavTx_SendRemote*` 旁）：

```c
/**
 * @brief 告警表：内容变化即发，否则按保活周期发；TUNNEL(0x8001) 打包 active 行。
 */
static Px4Lite_Result_t MavTx_SendRemoteAlarmTable(uint32_t now_ms)
{
  Px4Lite_AlarmSnapshot_t alarm;
  uint8_t payload[PX4LITE_TUNNEL_ALARM_MAX_BYTES];
  uint16_t plen;
  uint32_t sig;
  uint8_t changed;
  uint8_t keepalive_due;
  Px4Lite_Result_t result;

  if (Px4Lite_CopyAlarm(&alarm) != PX4LITE_OK) { return PX4LITE_NOT_READY; }

  sig           = Px4Lite_AlarmTableSignature(alarm.records, (uint8_t)PX4LITE_MODULE_COUNT);
  changed       = (sig != s_last_alarm_sig) ? 1U : 0U;
  keepalive_due = MavTx_TimeReached(now_ms, s_next_remote_alarm_ms);
  if ((changed == 0U) && (keepalive_due == 0U)) { return PX4LITE_IDLE; }

  if (changed != 0U) { s_alarm_ver++; }

  plen = Px4Lite_PackAlarmTable(alarm.records, (uint8_t)PX4LITE_MODULE_COUNT, s_alarm_ver, now_ms, payload, sizeof(payload));
  if (plen == 0U) { return PX4LITE_IO_ERROR; }

  (void)mavlink_msg_tunnel_pack_chan(PX4LITE_MAVLINK_SYSTEM_ID, PX4LITE_MAVLINK_COMPONENT_ID, MAVLINK_COMM_0,
                                     &s_message, 0U /*target_system 广播*/, 0U /*target_component*/,
                                     PX4LITE_TUNNEL_PT_ALARM_TABLE, (uint8_t)plen, payload);
  result = MavTx_SendPrepared();
  if (result == PX4LITE_OK) {
    s_last_alarm_sig      = sig;
    s_next_remote_alarm_ms = now_ms + PX4LITE_MAVLINK_REMOTE_ALARM_PERIOD_MS;
    s_stats.remote_alarm_count++;
  }
  return result;
}
```

顶部加 `#include "px4lite_remote_tunnel.h"`。

- [ ] **Step 4: 接入 slot 轮转**

在 `Px4Lite_MavlinkTxRun` 的 `switch` 内 `MAV_TX_SLOT_STATUSTEXT` 之后加：

```c
      case MAV_TX_SLOT_REMOTE_ALARM:
        if (PX4LITE_MAVLINK_ENABLE_REMOTE_ALARM == 0U) { continue; }
        result = MavTx_SendRemoteAlarmTable(now_ms);
        if ((result == PX4LITE_OK) || (result == PX4LITE_BUSY) || (result == PX4LITE_IO_ERROR)) { return result; }
        continue;
```

（计时与版本在发送函数内自管，故此处不调用 `MavTx_RecordResult`。）

- [ ] **Step 5: 编译期语法检查（无整机环境）**

Run（syntax-only，桩头 shadow CMSIS/HAL；FreeRTOS 不参与 tx.c）:
```bash
gcc -std=c99 -Wall -Wextra -fsyntax-only -I Tests/Unit/stubs -I Framework/Inc \
  -I Third_Party/mavlink -I Third_Party/mavlink/common Framework/Src/px4lite_mavlink_tx.c
```
Expected: 退出 0、无 error。（若 tx.c 依赖 FreeRTOS 头导致 syntax-only 失败，改为只核对本 Task 改动段落括号配平，并在 Keil 中编译验证。）

- [ ] **Step 6: 提交**

```bash
git add Framework/Inc/px4lite_config.h Framework/Src/px4lite_mavlink_tx.c Framework/Inc/px4lite_mavlink_tx.h
git commit -m "feat: 发送端按变化与保活发送TUNNEL告警表"
```

---

### Task 5: 集成验证与回归

**Files:** 无新增，跑全部相关桌面单测 + Keil 构建。

- [ ] **Step 1: 跑齐桌面单测**

```bash
for t in test_remote_tunnel_alarm test_app_display_alarm_remote test_mavlink_rx_alarm_tunnel; do
  echo "== $t =="; done
# 分别用各自 Task 的编译命令运行，全部应 PASS。
```
Expected: 三个测试全部 `PASS`。

- [ ] **Step 2: 既有单测回归**

确认 `Tests/Unit/test_app_display_model_remote_power.c`（电机电池）等既有测试仍 PASS（编译命令见各文件头/历史）。

- [ ] **Step 3: Keil 整机构建（用户侧）**

> 本机无 ARMCC/Keil，无法执行。交由实机侧：把新增的 `px4lite_remote_tunnel.c` 加入 Keil/EIDE 工程（`.eide/eide.yml` 的 Framework 组），`Rebuild All` 目标 `0 Error(s), 0 Warning(s)`。

- [ ] **Step 4: 实机联调（用户侧）**

REMOTE 模式下，制造目标设备多条告警，确认显示端告警页出现**多行**且与目标一致；断链后保留旧表并标 stale。

- [ ] **Step 5: 提交收尾（如有工程文件改动）**

```bash
git add .eide/eide.yml
git commit -m "build: 告警表TUNNEL编解码源文件接入工程"
```

---

## Self-Review

- **Spec coverage（LY/20 Part 1 §3–§9）**：数据流(§3)→Task1-4；载荷布局(§4)→Task1；on-change+保活(§5)→Task4；RemoteTelemetry 存储(§6)→Task2+3；App_GetDisplayAlarm REMOTE(§7)→Task2；测试(§8)→Task1/2/3+Task5；带宽(§9)→由 on-change+保活策略保证（Task4）。**消息日志(Part 2)不在本计划**，符合分期。
- **Placeholder scan**：各代码步均为完整代码；唯二运行期依赖真实字段名处（`App_AlarmRecord_t` 填充、`Px4Lite_LoRaRxFrame_t` 字段）已显式标注"按真实定义核对"，非占位逻辑。
- **Type consistency**：`Px4Lite_PackAlarmTable`/`Px4Lite_UnpackAlarmTable`/`Px4Lite_AlarmTableSignature` 签名在 Task1 定义、Task3/4 一致引用；`alarm_records/alarm_table_count/alarm_table_ver/alarm_table_update_ms` 在 Task2 定义、Task3 写入、Task2 测试读取，命名一致；payload_type `PX4LITE_TUNNEL_PT_ALARM_TABLE` 全程统一。
</content>
