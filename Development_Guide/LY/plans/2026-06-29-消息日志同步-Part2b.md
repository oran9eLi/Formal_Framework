# 消息日志远端同步 (Part 2b) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** REMOTE 模式下显示端按 `sequence` 增量同步目标设备的消息日志(允许丢帧空洞，断链保留并标 stale)；发送端按变化增量 + LOGSEQ 心跳发送 TUNNEL(0x8002)。

**Architecture:** 先把 2a 在业务层的日志环形缓冲**下沉到 Framework** 新模块 `px4lite_local_msglog`(单一真值源，TX/显示/App 都读它)，业务层 `app_message_log` 只留去抖/档位/生成策略并 push。再加 TUNNEL 0x8002 日志增量编解码(纯函数对)、RemoteTelemetry 远端日志环形缓冲 + RX 去重、TX `MAV_TX_SLOT_REMOTE_LOG`(增量游标 + 心跳)、`App_GetDisplayMessageLog(REMOTE)` 归一返回远端日志。两套 UI 经统一接口零改动(2a 的桥接已覆盖 REMOTE 分支)。

**Tech Stack:** C99、STM32F407、FreeRTOS、MAVLink v2(`mavlink_msg_tunnel.h`)、桌面 gcc 单测(MSYS2 gcc 16, `Tests/Unit/stubs/`)。

设计依据：`Development_Guide/LY/20_告警表与消息日志远端同步实现设计.md` Part 2（§12–§15）。前置：Part 2a 已完成(枚举上提、业务生产者、LOCAL 取数、display 桥接)。

## Global Constraints

- 无动态分配；固定容量缓冲；按 comm / biz 周期构建。
- 分层严格 Framework < Business：Framework **不得** include Business 头或调用 `App_*`。日志缓冲与 TUNNEL 编解码均在 Framework；业务层向下调用 Framework。
- 打包载体只用 `TUNNEL`，`payload_type=0x8002`=消息日志增量/心跳；载荷**小端**。
- 日志条目 8 字节：`sequence`(2)+`message_id`(2)+`time_hhmmss`(3)+`severity`(1)；表头 3 字节：`latest_seq`(2)+`count`(1)。`count=0` 为 LOGSEQ 心跳(仅 `latest_seq`)。
- 去重：RX 仅追加 `sequence > log_last_seq` 的条目；允许序号空洞(丢帧不补传)。
- 断链：App 层判 `STALE` 保留最后日志，不清零、不回退本机。
- `PX4LITE_REMOTE_LOG_CAP` = 9，与 `APP_DISPLAY_LOG_CAP` 一致(Framework 不引用 Business 常量，独立定义并在注释标注须一致)。
- 桌面单测用 `Tests/Unit/stubs/` 桩 shadow CMSIS/HAL。
- 每个 commit 信息以 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` 结尾。

## File Structure

- `Framework/Inc/px4lite_local_msglog.h` / `Framework/Src/px4lite_local_msglog.c`（新建）：`Px4Lite_LogEntry_t` + 本机待发/显示日志环形缓冲(Reset/Push/Copy/DrainSince)。
- `Business/Src/app_message_log.c`（改）：移除内部环形缓冲，改 push/copy 委托 Framework 模块；去抖/档位/生成策略保留。
- `Framework/Inc/px4lite_remote_tunnel.h` / `.c`（改）：加 0x8002 日志增量 pack/unpack。
- `Framework/Inc/px4lite_remote_telemetry.h`（改）：加 `VALID_LOG` 位 + 远端日志环形缓冲字段。
- `Framework/Src/px4lite_mavlink_rx.c`（改）：`MavlinkRx_HandleTunnel` 按 payload_type 分派，加 0x8002 去重写入。
- `Framework/Inc/px4lite_config.h` / `Framework/Src/px4lite_mavlink_tx.c`（改）：加 enable/周期宏 + `MAV_TX_SLOT_REMOTE_LOG`。
- `Business/Src/app_display_model.c`（改）：`App_GetDisplayMessageLog` REMOTE 分支返回远端日志。
- 测试：`test_local_msglog.c`、`test_remote_tunnel_log.c`、`test_mavlink_rx_log_tunnel.c`、`test_app_display_messagelog_remote.c`（新建）。

---

### Task 1: 日志环形缓冲下沉到 Framework + 重接 2a 业务生产者

**Files:**
- Create: `Framework/Inc/px4lite_local_msglog.h`、`Framework/Src/px4lite_local_msglog.c`
- Modify: `Business/Src/app_message_log.c`（删内部 ring，委托 Framework）
- Test: `Tests/Unit/test_local_msglog.c`；回归 `Tests/Unit/test_app_message_log.c`

**Interfaces:**
- Produces:
  - 类型 `Px4Lite_LogEntry_t { uint16_t sequence; uint16_t message_id; uint32_t time_hhmmss; uint16_t fault_code; uint8_t severity; uint8_t source_id; uint8_t active; uint8_t reserved[3]; }`
  - 常量 `PX4LITE_LOCAL_LOG_CAP`(=9)
  - `void Px4Lite_LocalMsgLogReset(void)`
  - `void Px4Lite_LocalMsgLogPush(uint16_t message_id, uint32_t time_hhmmss, uint16_t fault_code, uint8_t severity, uint8_t source_id, uint8_t active)`
  - `uint16_t Px4Lite_LocalMsgLogCopy(Px4Lite_LogEntry_t *out, uint16_t cap, uint32_t *out_version, uint16_t *out_last_seq)` — 旧→新拷出，返回条目数。
  - `uint16_t Px4Lite_LocalMsgLogDrainSince(uint16_t after_seq, Px4Lite_LogEntry_t *out, uint16_t max, uint16_t *out_latest_seq)` — 拷出 `sequence > after_seq` 的条目(旧→新)，返回条数，`out_latest_seq` 写当前最新序号(供心跳)。

- [ ] **Step 1: 写 Framework 头**

Create `Framework/Inc/px4lite_local_msglog.h`：

```c
/**
 * @file px4lite_local_msglog.h
 * @brief 本机消息日志环形缓冲(Framework 单一真值源)。
 *
 * @details
 * 业务层 app_message_log 负责去抖/生成并 Push；显示层经 App_MessageLogCopy 读取；
 * 远端同步 TX 经 DrainSince 取增量。Framework 层不依赖 Business。
 */
#ifndef PX4LITE_LOCAL_MSGLOG_H
#define PX4LITE_LOCAL_MSGLOG_H

#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

#define PX4LITE_LOCAL_LOG_CAP 9U /**< 本机日志容量；须与 Business APP_DISPLAY_LOG_CAP 一致。 */

/** @brief 一条结构化日志条目(与 Business App_DisplayLogEntry_t 字段同构)。 */
typedef struct {
  uint16_t sequence;     /**< 单调序号(从 1 起，跳过 0)。 */
  uint16_t message_id;   /**< 消息编号(App_LogMessageId_t 取值)。 */
  uint32_t time_hhmmss;  /**< 事件时间 HHMMSS。 */
  uint16_t fault_code;   /**< 关联故障码，0 无。 */
  uint8_t severity;      /**< 严重度。 */
  uint8_t source_id;     /**< 来源 ID。 */
  uint8_t active;        /**< 告警类触发/恢复标志。 */
  uint8_t reserved[3];   /**< 对齐保留。 */
} Px4Lite_LogEntry_t;

/** @brief 复位缓冲与序号。 */
void Px4Lite_LocalMsgLogReset(void);

/** @brief 追加一条日志(序号自增)。 */
void Px4Lite_LocalMsgLogPush(uint16_t message_id, uint32_t time_hhmmss,
                             uint16_t fault_code, uint8_t severity, uint8_t source_id, uint8_t active);

/**
 * @brief 拷出全部条目(旧→新)。
 * @return 条目数(<= cap)。out_version/out_last_seq 可空。
 */
uint16_t Px4Lite_LocalMsgLogCopy(Px4Lite_LogEntry_t *out, uint16_t cap,
                                 uint32_t *out_version, uint16_t *out_last_seq);

/**
 * @brief 拷出 sequence 大于 after_seq 的条目(旧→新)，供增量发送。
 * @param[out] out_latest_seq 当前最新序号(无条目为 0)，可空。
 * @return 拷出条数(<= max)。
 */
uint16_t Px4Lite_LocalMsgLogDrainSince(uint16_t after_seq, Px4Lite_LogEntry_t *out, uint16_t max,
                                       uint16_t *out_latest_seq);

#ifdef __cplusplus
}
#endif

#endif /* PX4LITE_LOCAL_MSGLOG_H */
```

- [ ] **Step 2: 写 Framework 实现**

Create `Framework/Src/px4lite_local_msglog.c`（环形缓冲迁自 2a app_message_log.c 的 ring 段）：

```c
/**
 * @file px4lite_local_msglog.c
 * @brief 本机消息日志环形缓冲实现。无动态分配、无浮点。
 */
#include "px4lite_local_msglog.h"

#include <string.h>

static Px4Lite_LogEntry_t s_ring[PX4LITE_LOCAL_LOG_CAP];
static uint16_t s_count;
static uint16_t s_head;       /* 下一个写入位置 */
static uint16_t s_next_seq;   /* 下一条序号(从 1 起) */
static uint16_t s_last_seq;   /* 最近一条已写序号(0=无) */
static uint32_t s_version;

void Px4Lite_LocalMsgLogReset(void)
{
  memset(s_ring, 0, sizeof(s_ring));
  s_count = 0U; s_head = 0U; s_next_seq = 1U; s_last_seq = 0U; s_version = 0U;
}

void Px4Lite_LocalMsgLogPush(uint16_t message_id, uint32_t time_hhmmss,
                             uint16_t fault_code, uint8_t severity, uint8_t source_id, uint8_t active)
{
  Px4Lite_LogEntry_t *e = &s_ring[s_head];
  memset(e, 0, sizeof(*e));
  e->sequence    = s_next_seq;
  e->message_id  = message_id;
  e->time_hhmmss = time_hhmmss;
  e->fault_code  = fault_code;
  e->severity    = severity;
  e->source_id   = source_id;
  e->active      = active;

  s_last_seq = s_next_seq;
  s_next_seq++;
  if (s_next_seq == 0U) { s_next_seq = 1U; }
  s_head = (uint16_t)((s_head + 1U) % PX4LITE_LOCAL_LOG_CAP);
  if (s_count < PX4LITE_LOCAL_LOG_CAP) { s_count++; }
  s_version++;
}

uint16_t Px4Lite_LocalMsgLogCopy(Px4Lite_LogEntry_t *out, uint16_t cap,
                                 uint32_t *out_version, uint16_t *out_last_seq)
{
  uint16_t i;
  uint16_t n = (s_count < cap) ? s_count : cap;
  uint16_t start;

  if (out_version != 0) { *out_version = s_version; }
  if (out_last_seq != 0) { *out_last_seq = s_last_seq; }
  if ((out == 0) || (n == 0U)) { return 0U; }

  start = (uint16_t)((s_head + PX4LITE_LOCAL_LOG_CAP - s_count) % PX4LITE_LOCAL_LOG_CAP);
  /* 若 cap < count，丢最旧、保最新 n 条。 */
  if (s_count > cap) { start = (uint16_t)((start + (s_count - cap)) % PX4LITE_LOCAL_LOG_CAP); }
  for (i = 0U; i < n; ++i) { out[i] = s_ring[(uint16_t)((start + i) % PX4LITE_LOCAL_LOG_CAP)]; }
  return n;
}

uint16_t Px4Lite_LocalMsgLogDrainSince(uint16_t after_seq, Px4Lite_LogEntry_t *out, uint16_t max,
                                       uint16_t *out_latest_seq)
{
  uint16_t i;
  uint16_t start;
  uint16_t written = 0U;

  if (out_latest_seq != 0) { *out_latest_seq = s_last_seq; }
  if ((out == 0) || (max == 0U) || (s_count == 0U)) { return 0U; }

  start = (uint16_t)((s_head + PX4LITE_LOCAL_LOG_CAP - s_count) % PX4LITE_LOCAL_LOG_CAP);
  for (i = 0U; (i < s_count) && (written < max); ++i) {
    const Px4Lite_LogEntry_t *e = &s_ring[(uint16_t)((start + i) % PX4LITE_LOCAL_LOG_CAP)];
    if (e->sequence > after_seq) { out[written] = *e; written++; }
  }
  return written;
}
```

> 序号回绕说明：`> after_seq` 比较在 16 位序号回绕(每 65535 条)后会有一次误判窗口；本机日志按状态变化生成、量级远小于 65535，且断链恢复靠整表 Copy 兜底，故不做回绕处理(与告警表 ver 处理一致)。

- [ ] **Step 3: 写 Framework 单测**

Create `Tests/Unit/test_local_msglog.c`：

```c
/**
 * @file test_local_msglog.c
 * @brief 本机日志环形缓冲：序号单调、旧→新拷出、增量 DrainSince、容量回卷。
 */
#include <stdint.h>
#include <stdio.h>
#include <string.h>

#include "px4lite_local_msglog.h"

static int g_fail;
static void Eq(const char *n, uint32_t a, uint32_t e)
{ if (a != e) { printf("FAIL %s actual=%lu expected=%lu\n", n, (unsigned long)a, (unsigned long)e); g_fail++; } }

int main(void)
{
  Px4Lite_LogEntry_t buf[PX4LITE_LOCAL_LOG_CAP];
  uint16_t n, latest = 0U;
  uint16_t i;

  Px4Lite_LocalMsgLogReset();
  for (i = 0U; i < 3U; ++i) { Px4Lite_LocalMsgLogPush((uint16_t)(10U + i), 1000U + i, 0U, 0U, 0U, 0U); }

  n = Px4Lite_LocalMsgLogCopy(buf, PX4LITE_LOCAL_LOG_CAP, 0, &latest);
  Eq("copy n", n, 3U);
  Eq("latest", latest, 3U);
  Eq("seq0", buf[0].sequence, 1U);
  Eq("seq2", buf[2].sequence, 3U);
  Eq("mid0", buf[0].message_id, 10U);

  /* DrainSince(1) 只回 seq>1 的两条。 */
  n = Px4Lite_LocalMsgLogDrainSince(1U, buf, PX4LITE_LOCAL_LOG_CAP, &latest);
  Eq("drain n", n, 2U);
  Eq("drain first seq", buf[0].sequence, 2U);
  Eq("drain latest", latest, 3U);

  /* 容量回卷：再压 8 条(共 11)，只留最新 9，最旧应为 seq3。 */
  for (i = 0U; i < 8U; ++i) { Px4Lite_LocalMsgLogPush(20U, 2000U, 0U, 0U, 0U, 0U); }
  n = Px4Lite_LocalMsgLogCopy(buf, PX4LITE_LOCAL_LOG_CAP, 0, &latest);
  Eq("wrap n", n, PX4LITE_LOCAL_LOG_CAP);
  Eq("wrap oldest seq", buf[0].sequence, 3U);
  Eq("wrap latest", latest, 11U);

  if (g_fail == 0) { printf("PASS test_local_msglog\n"); return 0; }
  printf("FAIL test_local_msglog fails=%d\n", g_fail); return 1;
}
```

- [ ] **Step 4: 运行确认失败，再写实现后通过**

```bash
gcc -std=c99 -Wall -Wextra -I Framework/Inc Tests/Unit/test_local_msglog.c Framework/Src/px4lite_local_msglog.c -o /tmp/t_llog.exe && /tmp/t_llog.exe
```
先因缺 .c 失败；Step 2 完成后应 `PASS test_local_msglog`。

- [ ] **Step 5: 业务生产者改为委托 Framework**

在 `Business/Src/app_message_log.c`：
1. 顶部加 `#include "px4lite_local_msglog.h"`。
2. **删除**内部 ring 静态量(`s_ring`/`s_count`/`s_head`/`s_next_seq`/`s_version`)与 `AppLog_Push` 中写 ring 的逻辑、`App_MessageLogCopy` 的环形展开逻辑。
3. `AppLog_Push` 改为：
```c
static void AppLog_Push(App_LogMessageId_t msg, uint32_t now_ms)
{
  Px4Lite_LocalMsgLogPush((uint16_t)msg, AppLog_Hhmmss(now_ms),
                          0U, 0U, 0U, (uint8_t)((msg == APP_LOGMSG_ALARM_ACTIVE) ? 1U : 0U));
}
```
4. `App_MessageLogInit` 中把 ring 复位替换为 `Px4Lite_LocalMsgLogReset();`（保留去抖/band/boot 状态复位不变）。
5. `App_MessageLogCopy` 改为委托 + 类型映射：
```c
Px4Lite_Result_t App_MessageLogCopy(App_DisplayLogSnapshot_t *out)
{
  Px4Lite_LogEntry_t tmp[PX4LITE_LOCAL_LOG_CAP];
  uint16_t n, i, last = 0U;
  uint32_t ver = 0U;

  if (out == 0) { return PX4LITE_INVALID_PARAM; }
  memset(out, 0, sizeof(*out));
  n = Px4Lite_LocalMsgLogCopy(tmp, PX4LITE_LOCAL_LOG_CAP, &ver, &last);
  out->version = ver;
  out->count   = n;
  if (n == 0U) { return PX4LITE_NOT_READY; }
  if (n > (uint16_t)APP_DISPLAY_LOG_CAP) { n = (uint16_t)APP_DISPLAY_LOG_CAP; out->count = n; }
  for (i = 0U; i < n; ++i) {
    out->entries[i].sequence    = tmp[i].sequence;
    out->entries[i].message_id  = tmp[i].message_id;
    out->entries[i].time_hhmmss = tmp[i].time_hhmmss;
    out->entries[i].fault_code  = tmp[i].fault_code;
    out->entries[i].severity    = tmp[i].severity;
    out->entries[i].source_id   = tmp[i].source_id;
    out->entries[i].active      = tmp[i].active;
  }
  return PX4LITE_OK;
}
```

> `PX4LITE_LOCAL_LOG_CAP` 与 `APP_DISPLAY_LOG_CAP` 均为 9，映射不丢条。

- [ ] **Step 6: 回归 2a 生产者单测 + 提交**

```bash
gcc -std=c99 -Wall -Wextra -I Tests/Unit/stubs -I Business/Inc -I Framework/Inc \
  Tests/Unit/test_app_message_log.c Business/Src/app_message_log.c Framework/Src/px4lite_local_msglog.c -o /tmp/t_log.exe && /tmp/t_log.exe
gcc -std=c99 -Wall -Wextra -I Framework/Inc Tests/Unit/test_local_msglog.c Framework/Src/px4lite_local_msglog.c -o /tmp/t_llog.exe && /tmp/t_llog.exe
```
两者均 PASS（生产者行为不变，存储下沉）。

```bash
git add Framework/Inc/px4lite_local_msglog.h Framework/Src/px4lite_local_msglog.c Business/Src/app_message_log.c Tests/Unit/test_local_msglog.c
git commit -m "refactor: 本机消息日志缓冲下沉到Framework单一真值源"
```

> 注：`test_app_message_log.c` 等链接 `app_message_log.c` 的命令此后都需追加 `Framework/Src/px4lite_local_msglog.c`。

---

### Task 2: TUNNEL 0x8002 日志增量编解码 + 单测

**Files:**
- Modify: `Framework/Inc/px4lite_remote_tunnel.h`、`Framework/Src/px4lite_remote_tunnel.c`
- Test: `Tests/Unit/test_remote_tunnel_log.c`

**Interfaces:**
- Consumes: `Px4Lite_LogEntry_t`（`px4lite_local_msglog.h`）。
- Produces:
  - 宏 `PX4LITE_TUNNEL_PT_MESSAGE_LOG 0x8002U`、`PX4LITE_TUNNEL_LOG_HEADER_BYTES 3U`、`PX4LITE_TUNNEL_LOG_ENTRY_BYTES 8U`、`PX4LITE_TUNNEL_LOG_MAX_ENTRIES`、`PX4LITE_TUNNEL_LOG_MAX_BYTES`。
  - `uint16_t Px4Lite_PackMessageLog(const Px4Lite_LogEntry_t *entries, uint8_t count, uint16_t latest_seq, uint8_t *out, uint16_t out_cap)`
  - `Px4Lite_Result_t Px4Lite_UnpackMessageLog(const uint8_t *payload, uint16_t len, Px4Lite_LogEntry_t *out_entries, uint8_t out_cap, uint8_t *out_count, uint16_t *out_latest_seq)`

- [ ] **Step 1: 头文件加 0x8002 契约**

在 `Framework/Inc/px4lite_remote_tunnel.h`：顶部加 `#include "px4lite_local_msglog.h"`；在 ALARM 宏之后加：

```c
#define PX4LITE_TUNNEL_PT_MESSAGE_LOG 0x8002U /**< TUNNEL payload_type：消息日志增量/心跳。 */

#define PX4LITE_TUNNEL_LOG_HEADER_BYTES 3U /**< latest_seq(2)+count(1)。 */
#define PX4LITE_TUNNEL_LOG_ENTRY_BYTES  8U /**< sequence(2)+message_id(2)+time_hhmmss(3)+severity(1)。 */
#define PX4LITE_TUNNEL_LOG_MAX_ENTRIES  ((uint8_t)PX4LITE_LOCAL_LOG_CAP)
#define PX4LITE_TUNNEL_LOG_MAX_BYTES \
  (PX4LITE_TUNNEL_LOG_HEADER_BYTES + (uint16_t)PX4LITE_TUNNEL_LOG_ENTRY_BYTES * PX4LITE_TUNNEL_LOG_MAX_ENTRIES)

/**
 * @brief 打包日志增量载荷(小端)。count=0 即仅 latest_seq 的 LOGSEQ 心跳。
 * @return payload 字节数；out 为空或容量不足表头返回 0；容量不足时截断到整条。
 */
uint16_t Px4Lite_PackMessageLog(const Px4Lite_LogEntry_t *entries, uint8_t count, uint16_t latest_seq,
                                uint8_t *out, uint16_t out_cap);

/**
 * @brief 解包日志增量载荷。
 * @return PX4LITE_OK 成功；PX4LITE_INVALID_PARAM 空指针/长度不足/条目越界。
 */
Px4Lite_Result_t Px4Lite_UnpackMessageLog(const uint8_t *payload, uint16_t len,
                                          Px4Lite_LogEntry_t *out_entries, uint8_t out_cap,
                                          uint8_t *out_count, uint16_t *out_latest_seq);
```

- [ ] **Step 2: 实现**

在 `Framework/Src/px4lite_remote_tunnel.c` 末尾追加(复用文件内已有 `Tun_PutU16`/`Tun_GetU16`)：

```c
static void Tun_PutU24(uint8_t *p, uint32_t v)
{ p[0] = (uint8_t)(v & 0xFFU); p[1] = (uint8_t)((v >> 8) & 0xFFU); p[2] = (uint8_t)((v >> 16) & 0xFFU); }
static uint32_t Tun_GetU24(const uint8_t *p)
{ return (uint32_t)p[0] | ((uint32_t)p[1] << 8) | ((uint32_t)p[2] << 16); }

uint16_t Px4Lite_PackMessageLog(const Px4Lite_LogEntry_t *entries, uint8_t count, uint16_t latest_seq,
                                uint8_t *out, uint16_t out_cap)
{
  uint16_t off = PX4LITE_TUNNEL_LOG_HEADER_BYTES;
  uint8_t n = 0U;
  uint8_t i;

  if ((out == 0) || (out_cap < PX4LITE_TUNNEL_LOG_HEADER_BYTES)) { return 0U; }

  for (i = 0U; (i < count) && (entries != 0); ++i) {
    uint8_t *row;
    if ((uint16_t)(off + PX4LITE_TUNNEL_LOG_ENTRY_BYTES) > out_cap) { break; }
    row = &out[off];
    Tun_PutU16(&row[0], entries[i].sequence);
    Tun_PutU16(&row[2], entries[i].message_id);
    Tun_PutU24(&row[4], entries[i].time_hhmmss);
    row[7] = entries[i].severity;
    off = (uint16_t)(off + PX4LITE_TUNNEL_LOG_ENTRY_BYTES);
    n++;
  }

  Tun_PutU16(&out[0], latest_seq);
  out[2] = n;
  return off;
}

Px4Lite_Result_t Px4Lite_UnpackMessageLog(const uint8_t *payload, uint16_t len,
                                          Px4Lite_LogEntry_t *out_entries, uint8_t out_cap,
                                          uint8_t *out_count, uint16_t *out_latest_seq)
{
  uint16_t off = PX4LITE_TUNNEL_LOG_HEADER_BYTES;
  uint8_t stored = 0U;
  uint8_t n;
  uint8_t i;

  if ((payload == 0) || (out_entries == 0) || (out_count == 0) || (out_latest_seq == 0)) { return PX4LITE_INVALID_PARAM; }
  if (len < PX4LITE_TUNNEL_LOG_HEADER_BYTES) { return PX4LITE_INVALID_PARAM; }

  *out_latest_seq = Tun_GetU16(&payload[0]);
  n = payload[2];

  for (i = 0U; i < n; ++i) {
    const uint8_t *row;
    if ((uint16_t)(off + PX4LITE_TUNNEL_LOG_ENTRY_BYTES) > len) { return PX4LITE_INVALID_PARAM; }
    if (stored < out_cap) {
      row = &payload[off];
      memset(&out_entries[stored], 0, sizeof(out_entries[stored]));
      out_entries[stored].sequence    = Tun_GetU16(&row[0]);
      out_entries[stored].message_id  = Tun_GetU16(&row[2]);
      out_entries[stored].time_hhmmss = Tun_GetU24(&row[4]);
      out_entries[stored].severity    = row[7];
      stored++;
    }
    off = (uint16_t)(off + PX4LITE_TUNNEL_LOG_ENTRY_BYTES);
  }

  *out_count = stored;
  return PX4LITE_OK;
}
```

- [ ] **Step 3: 单测(round-trip / 心跳 / 截断拒绝)**

Create `Tests/Unit/test_remote_tunnel_log.c`：

```c
/**
 * @file test_remote_tunnel_log.c
 * @brief 日志增量 TUNNEL(0x8002) 打包/解包 round-trip 与心跳。
 */
#include <stdint.h>
#include <stdio.h>
#include <string.h>

#include "px4lite_remote_tunnel.h"

static int g_fail;
static void Eq(const char *n, uint32_t a, uint32_t e)
{ if (a != e) { printf("FAIL %s actual=%lu expected=%lu\n", n, (unsigned long)a, (unsigned long)e); g_fail++; } }

int main(void)
{
  Px4Lite_LogEntry_t in[2];
  Px4Lite_LogEntry_t out[PX4LITE_TUNNEL_LOG_MAX_ENTRIES];
  uint8_t buf[PX4LITE_TUNNEL_LOG_MAX_BYTES];
  uint8_t cnt = 0xFFU;
  uint16_t latest = 0U, len;

  memset(in, 0, sizeof(in));
  in[0].sequence = 7U; in[0].message_id = 4U; in[0].time_hhmmss = 123456U; in[0].severity = 2U;
  in[1].sequence = 8U; in[1].message_id = 12U; in[1].time_hhmmss = 235959U; in[1].severity = 1U;

  len = Px4Lite_PackMessageLog(in, 2U, 8U, buf, sizeof(buf));
  Eq("len", len, PX4LITE_TUNNEL_LOG_HEADER_BYTES + 2U * PX4LITE_TUNNEL_LOG_ENTRY_BYTES);
  Eq("hdr count", buf[2], 2U);

  Eq("unpack ok", Px4Lite_UnpackMessageLog(buf, len, out, PX4LITE_TUNNEL_LOG_MAX_ENTRIES, &cnt, &latest), PX4LITE_OK);
  Eq("count", cnt, 2U);
  Eq("latest", latest, 8U);
  Eq("e0 seq", out[0].sequence, 7U);
  Eq("e0 mid", out[0].message_id, 4U);
  Eq("e0 time", out[0].time_hhmmss, 123456U);
  Eq("e1 time", out[1].time_hhmmss, 235959U);
  Eq("e1 sev", out[1].severity, 1U);

  /* 心跳：count=0，只带 latest_seq。 */
  len = Px4Lite_PackMessageLog(0, 0U, 42U, buf, sizeof(buf));
  Eq("hb len", len, PX4LITE_TUNNEL_LOG_HEADER_BYTES);
  cnt = 0xFFU; latest = 0U;
  Eq("hb unpack", Px4Lite_UnpackMessageLog(buf, len, out, PX4LITE_TUNNEL_LOG_MAX_ENTRIES, &cnt, &latest), PX4LITE_OK);
  Eq("hb count", cnt, 0U);
  Eq("hb latest", latest, 42U);

  /* 截断拒绝：声称 2 条但长度不足。 */
  { uint8_t bad[5] = {1U,0U,2U,0U,0U};
    Eq("short rejected", Px4Lite_UnpackMessageLog(bad, 5U, out, PX4LITE_TUNNEL_LOG_MAX_ENTRIES, &cnt, &latest), PX4LITE_INVALID_PARAM); }

  if (g_fail == 0) { printf("PASS test_remote_tunnel_log\n"); return 0; }
  printf("FAIL test_remote_tunnel_log fails=%d\n", g_fail); return 1;
}
```

- [ ] **Step 4: 运行 + 提交**

```bash
gcc -std=c99 -Wall -Wextra -I Tests/Unit/stubs -I Framework/Inc \
  Tests/Unit/test_remote_tunnel_log.c Framework/Src/px4lite_remote_tunnel.c -o /tmp/t_tlog.exe && /tmp/t_tlog.exe
```
Expected: `PASS test_remote_tunnel_log`。

```bash
git add Framework/Inc/px4lite_remote_tunnel.h Framework/Src/px4lite_remote_tunnel.c Tests/Unit/test_remote_tunnel_log.c
git commit -m "feat: 新增消息日志增量TUNNEL编解码纯函数与单测"
```

---

### Task 3: RemoteTelemetry 日志缓冲 + RX 0x8002 去重

**Files:**
- Modify: `Framework/Inc/px4lite_remote_telemetry.h`（VALID_LOG 位 + 日志字段）
- Modify: `Framework/Src/px4lite_mavlink_rx.c`（`MavlinkRx_HandleTunnel` 按 payload_type 分派 + 去重写入）
- Test: `Tests/Unit/test_mavlink_rx_log_tunnel.c`

**Interfaces:**
- Consumes: `Px4Lite_UnpackMessageLog`、`Px4Lite_LogEntry_t`、`Px4Lite_RemoteTelemetryMutable/Commit`。
- Produces: 快照新增 `log_entries[PX4LITE_REMOTE_LOG_CAP]`/`log_count`/`log_last_seq`/`log_update_ms`；RX 0x8002 去重追加。

- [ ] **Step 1: 加 VALID_LOG 位与快照字段**

在 `Framework/Inc/px4lite_remote_telemetry.h`：
顶部加 `#include "px4lite_local_msglog.h"`。VALID 位区(ALARM 之后)加：
```c
#define PX4LITE_REMOTE_VALID_LOG         (1UL << 11) /**< 远端消息日志字段有效。 */
```
定义容量(文件内合适处)：
```c
#define PX4LITE_REMOTE_LOG_CAP PX4LITE_LOCAL_LOG_CAP /**< 远端日志容量，与本机一致(9)。 */
```
在 `Px4Lite_RemoteTelemetrySnapshot_t` 内 alarm 字段块之后加：
```c
  Px4Lite_LogEntry_t log_entries[PX4LITE_REMOTE_LOG_CAP]; /**< 远端日志(旧→新)。 */
  uint16_t log_count;       /**< 有效条目数。 */
  uint16_t log_last_seq;    /**< 已接收的最高日志序号。 */
  uint32_t log_update_ms;   /**< 日志最近更新时间，单位 ms。 */
```

- [ ] **Step 2: RX 分派 + 去重写入**

把 `Framework/Src/px4lite_mavlink_rx.c` 的 `MavlinkRx_HandleTunnel`(行 342-363) 重构为按 payload_type 分派，新增 0x8002 分支：

```c
static Px4Lite_Result_t MavlinkRx_HandleTunnelAlarm(const mavlink_tunnel_t *tun, const mavlink_message_t *msg, uint32_t now_ms)
{
  Px4Lite_RemoteTelemetrySnapshot_t *snapshot;
  uint8_t count = 0U, ver = 0U;

  snapshot = Px4Lite_RemoteTelemetryMutable();
  if (Px4Lite_UnpackAlarmTable(tun->payload, tun->payload_length, now_ms,
                               snapshot->alarm_records, (uint8_t)PX4LITE_MODULE_COUNT, &count, &ver) != PX4LITE_OK) {
    return PX4LITE_IO_ERROR;
  }
  snapshot->alarm_table_count     = count;
  snapshot->alarm_table_ver       = ver;
  snapshot->alarm_table_update_ms = now_ms;
  Px4Lite_RemoteTelemetryCommit(msg->sysid, msg->compid, PX4LITE_REMOTE_VALID_ALARM, now_ms);
  return PX4LITE_OK;
}

static Px4Lite_Result_t MavlinkRx_HandleTunnelLog(const mavlink_tunnel_t *tun, const mavlink_message_t *msg, uint32_t now_ms)
{
  Px4Lite_RemoteTelemetrySnapshot_t *snapshot;
  Px4Lite_LogEntry_t rx[PX4LITE_TUNNEL_LOG_MAX_ENTRIES];
  uint8_t count = 0U;
  uint16_t latest = 0U;
  uint8_t i;

  if (Px4Lite_UnpackMessageLog(tun->payload, tun->payload_length, rx,
                               PX4LITE_TUNNEL_LOG_MAX_ENTRIES, &count, &latest) != PX4LITE_OK) {
    return PX4LITE_IO_ERROR;
  }

  snapshot = Px4Lite_RemoteTelemetryMutable();
  /* 去重追加：仅 sequence > log_last_seq 的条目，允许序号空洞。 */
  for (i = 0U; i < count; ++i) {
    if (rx[i].sequence > snapshot->log_last_seq) {
      uint16_t pos;
      if (snapshot->log_count < (uint16_t)PX4LITE_REMOTE_LOG_CAP) {
        pos = snapshot->log_count;
        snapshot->log_count++;
      } else {
        /* 满则左移丢最旧。 */
        uint16_t k;
        for (k = 1U; k < (uint16_t)PX4LITE_REMOTE_LOG_CAP; ++k) { snapshot->log_entries[k - 1U] = snapshot->log_entries[k]; }
        pos = (uint16_t)(PX4LITE_REMOTE_LOG_CAP - 1U);
      }
      snapshot->log_entries[pos] = rx[i];
      snapshot->log_last_seq = rx[i].sequence;
    }
  }
  /* count=0 心跳：仅对齐认知，不补数据；仍刷新时间与有效位。 */
  snapshot->log_update_ms = now_ms;
  Px4Lite_RemoteTelemetryCommit(msg->sysid, msg->compid, PX4LITE_REMOTE_VALID_LOG, now_ms);
  return PX4LITE_OK;
}

static Px4Lite_Result_t MavlinkRx_HandleTunnel(const mavlink_message_t *msg, uint32_t now_ms)
{
  mavlink_tunnel_t tun;

  mavlink_msg_tunnel_decode(msg, &tun);
  switch (tun.payload_type) {
    case PX4LITE_TUNNEL_PT_ALARM_TABLE:   return MavlinkRx_HandleTunnelAlarm(&tun, msg, now_ms);
    case PX4LITE_TUNNEL_PT_MESSAGE_LOG:   return MavlinkRx_HandleTunnelLog(&tun, msg, now_ms);
    default:                              return PX4LITE_IDLE;
  }
}
```

- [ ] **Step 3: 单测(去重/空洞/心跳)**

Create `Tests/Unit/test_mavlink_rx_log_tunnel.c`（构造 0x8002 帧 → RX → 检查远端快照去重）：

```c
/**
 * @file test_mavlink_rx_log_tunnel.c
 * @brief RX 收到 TUNNEL(0x8002) 后去重追加远端日志。
 */
#include <stdint.h>
#include <stdio.h>
#include <string.h>

#include "common/mavlink.h"
#include "px4lite_mavlink_rx.h"
#include "px4lite_remote_telemetry.h"
#include "px4lite_remote_tunnel.h"
#include "px4lite_config.h"

static int g_fail;
static void Eq(const char *n, uint32_t a, uint32_t e)
{ if (a != e) { printf("FAIL %s actual=%lu expected=%lu\n", n, (unsigned long)a, (unsigned long)e); g_fail++; } }

static void SendLog(const Px4Lite_LogEntry_t *e, uint8_t cnt, uint16_t latest, uint32_t now)
{
  uint8_t payload[PX4LITE_TUNNEL_LOG_MAX_BYTES];
  uint16_t plen = Px4Lite_PackMessageLog(e, cnt, latest, payload, sizeof(payload));
  mavlink_message_t msg;
  uint8_t buf[300];
  uint16_t flen;
  Px4Lite_LoRaRxFrame_t frame;

  (void)mavlink_msg_tunnel_pack_chan(2U, 1U, MAVLINK_COMM_0, &msg, PX4LITE_MAVLINK_SYSTEM_ID, 0U,
                                     PX4LITE_TUNNEL_PT_MESSAGE_LOG, (uint8_t)plen, payload);
  flen = mavlink_msg_to_send_buffer(buf, &msg);
  memset(&frame, 0, sizeof(frame));
  frame.frame_len = (uint16_t)flen; frame.system_id = 2U;
  memcpy(frame.data, buf, flen);
  (void)Px4Lite_MavlinkRxHandleFrame(&frame, now);
}

int main(void)
{
  uint32_t now = 8000U;
  Px4Lite_LogEntry_t e[3];
  Px4Lite_RemoteTelemetrySnapshot_t out;

  Px4Lite_RemoteTelemetryInit(now);
  (void)Px4Lite_RemoteTelemetrySetMode(PX4LITE_REMOTE_MODE_REMOTE, now);
  Px4Lite_MavlinkRxInit(now);

  memset(e, 0, sizeof(e));
  e[0].sequence = 1U; e[0].message_id = 0U; e[0].time_hhmmss = 100U;
  e[1].sequence = 2U; e[1].message_id = 4U; e[1].time_hhmmss = 200U;
  SendLog(e, 2U, 2U, now);

  Eq("copy ok", Px4Lite_RemoteTelemetryCopySnapshot(&out, now), PX4LITE_OK);
  Eq("count", out.log_count, 2U);
  Eq("last_seq", out.log_last_seq, 2U);
  Eq("e1 mid", out.log_entries[1].message_id, 4U);

  /* 重发 seq1/2(重复) + 新 seq3 → 只追加 seq3。 */
  e[2].sequence = 3U; e[2].message_id = 6U; e[2].time_hhmmss = 300U;
  SendLog(e, 3U, 3U, now + 100U);
  (void)Px4Lite_RemoteTelemetryCopySnapshot(&out, now + 100U);
  Eq("dedup count", out.log_count, 3U);
  Eq("dedup last", out.log_last_seq, 3U);

  /* 心跳 count=0：不改数据。 */
  SendLog(0, 0U, 3U, now + 200U);
  (void)Px4Lite_RemoteTelemetryCopySnapshot(&out, now + 200U);
  Eq("hb count unchanged", out.log_count, 3U);

  if (g_fail == 0) { printf("PASS test_mavlink_rx_log_tunnel\n"); return 0; }
  printf("FAIL test_mavlink_rx_log_tunnel fails=%d\n", g_fail); return 1;
}
```

- [ ] **Step 4: 运行 + 提交**

```bash
gcc -std=c99 -Wall -Wextra -I Tests/Unit/stubs -I Framework/Inc -I Third_Party/mavlink -I Third_Party/mavlink/common \
  Tests/Unit/test_mavlink_rx_log_tunnel.c Framework/Src/px4lite_mavlink_rx.c Framework/Src/px4lite_remote_telemetry.c \
  Framework/Src/px4lite_remote_tunnel.c Framework/Src/px4lite_local_msglog.c -o /tmp/t_rxlog.exe && /tmp/t_rxlog.exe
```
Expected: `PASS test_mavlink_rx_log_tunnel`。若 `Px4Lite_LoRaRxFrame_t` 字段名不符，按真实定义(`frame_len`/`system_id`/`data`)调整。

```bash
git add Framework/Inc/px4lite_remote_telemetry.h Framework/Src/px4lite_mavlink_rx.c Tests/Unit/test_mavlink_rx_log_tunnel.c
git commit -m "feat: 接收端解包TUNNEL消息日志去重写入远端快照"
```

---

### Task 4: TX —— 消息日志槽(增量 + LOGSEQ 心跳)

**Files:**
- Modify: `Framework/Inc/px4lite_config.h`（enable + 周期宏）
- Modify: `Framework/Src/px4lite_mavlink_tx.c`（slot + 发送函数 + 游标静态量 + stats）

**Interfaces:**
- Consumes: `Px4Lite_LocalMsgLogDrainSince`、`Px4Lite_PackMessageLog`、`mavlink_msg_tunnel_pack_chan`、`MavTx_SendPrepared`、`MavTx_TimeReached`。
- Produces: 有新条目即增量发 TUNNEL(0x8002)，否则每周期保活发 count=0 心跳。

- [ ] **Step 1: 加配置宏**

在 `Framework/Inc/px4lite_config.h` 的 REMOTE_ALARM 宏旁加：
```c
#define PX4LITE_MAVLINK_ENABLE_REMOTE_LOG    1U
#define PX4LITE_MAVLINK_REMOTE_LOG_PERIOD_MS 2000U /**< 日志 LOGSEQ 心跳周期；增量不受此限。 */
```

- [ ] **Step 2: slot 枚举 + 静态量 + stats**

在 `px4lite_mavlink_tx.c`：枚举 `MavTx_Slot_t` 中 `MAV_TX_SLOT_REMOTE_ALARM` 之后插入 `MAV_TX_SLOT_REMOTE_LOG,`。静态量区加：
```c
static uint32_t s_next_remote_log_ms;
static uint16_t s_last_sent_log_seq;
```
`Px4Lite_MavlinkTxInit` 内初始化：`s_next_remote_log_ms = now_ms; s_last_sent_log_seq = 0U;`。stats 结构加 `uint32_t remote_log_count;`。顶部确认已 `#include "px4lite_remote_tunnel.h"`(Part 1 已加)与 `#include "px4lite_local_msglog.h"`。

- [ ] **Step 3: 发送函数**

在 `px4lite_mavlink_tx.c`(放在 `MavTx_SendRemoteAlarmTable` 旁)：
```c
/**
 * @brief 消息日志：有新条目即增量发，否则按周期发 count=0 的 LOGSEQ 心跳。
 */
static Px4Lite_Result_t MavTx_SendRemoteLog(uint32_t now_ms)
{
  Px4Lite_LogEntry_t entries[PX4LITE_TUNNEL_LOG_MAX_ENTRIES];
  uint8_t payload[PX4LITE_TUNNEL_LOG_MAX_BYTES];
  uint16_t latest = 0U;
  uint16_t n;
  uint16_t plen;
  uint8_t keepalive_due;
  Px4Lite_Result_t result;

  n = Px4Lite_LocalMsgLogDrainSince(s_last_sent_log_seq, entries, PX4LITE_TUNNEL_LOG_MAX_ENTRIES, &latest);
  keepalive_due = MavTx_TimeReached(now_ms, s_next_remote_log_ms);
  if ((n == 0U) && (keepalive_due == 0U)) { return PX4LITE_IDLE; }

  plen = Px4Lite_PackMessageLog(entries, (uint8_t)n, latest, payload, sizeof(payload));
  if (plen == 0U) { return PX4LITE_IO_ERROR; }

  (void)mavlink_msg_tunnel_pack_chan(PX4LITE_MAVLINK_SYSTEM_ID, PX4LITE_MAVLINK_COMPONENT_ID, MAVLINK_COMM_0,
                                     &s_message, 0U, 0U,
                                     PX4LITE_TUNNEL_PT_MESSAGE_LOG, (uint8_t)plen, payload);
  result = MavTx_SendPrepared();
  if (result == PX4LITE_OK) {
    if (n > 0U) { s_last_sent_log_seq = entries[n - 1U].sequence; }
    s_next_remote_log_ms = now_ms + PX4LITE_MAVLINK_REMOTE_LOG_PERIOD_MS;
    s_stats.remote_log_count++;
  }
  return result;
}
```

- [ ] **Step 4: 接入轮转**

在 `Px4Lite_MavlinkTxRun` 的 `switch` 内 `MAV_TX_SLOT_REMOTE_ALARM` 之后加：
```c
      case MAV_TX_SLOT_REMOTE_LOG:
        if (PX4LITE_MAVLINK_ENABLE_REMOTE_LOG == 0U) { continue; }
        result = MavTx_SendRemoteLog(now_ms);
        if ((result == PX4LITE_OK) || (result == PX4LITE_BUSY) || (result == PX4LITE_IO_ERROR)) { return result; }
        continue;
```

- [ ] **Step 5: 语法核验(无整机)**

```bash
gcc -std=c99 -Wall -Wextra -fsyntax-only -I Tests/Unit/stubs -I Framework/Inc \
  -I Third_Party/mavlink -I Third_Party/mavlink/common Framework/Src/px4lite_mavlink_tx.c
```
Expected: 退出 0 无 error。若依赖 FreeRTOS 头致 syntax-only 失败，改为核对本 Task 改动段落括号配平，留待 Keil 验证。

- [ ] **Step 6: 提交**

```bash
git add Framework/Inc/px4lite_config.h Framework/Src/px4lite_mavlink_tx.c Framework/Inc/px4lite_mavlink_tx.h
git commit -m "feat: 发送端增量与心跳发送TUNNEL消息日志"
```

---

### Task 5: App_GetDisplayMessageLog(REMOTE) 返回远端日志

**Files:**
- Modify: `Business/Src/app_display_model.c`（REMOTE 分支）
- Test: `Tests/Unit/test_app_display_messagelog_remote.c`

**Interfaces:**
- Consumes: `App_CopyRemoteTelemetry`(经现有 `AppDisplay_RemoteDomain`)、`PX4LITE_REMOTE_VALID_LOG`。
- Produces: REMOTE 返回远端 `log_entries` 归一到 `App_DisplayLogSnapshot_t`(`version` 取 `log_last_seq`)。

- [ ] **Step 1: 写失败测试**

Create `Tests/Unit/test_app_display_messagelog_remote.c`（仿 `test_app_display_alarm_remote.c` 用真实 RemoteTelemetry 布置远端快照）：

```c
/**
 * @file test_app_display_messagelog_remote.c
 * @brief REMOTE 模式 App_GetDisplayMessageLog 返回远端日志。
 */
#include <stdint.h>
#include <stdio.h>
#include <string.h>

#include "app_display_model.h"
#include "app_message_log.h"
#include "px4lite_remote_telemetry.h"

static int g_fail;
static Px4Lite_RemoteMode_t s_mode;
static void Eq(const char *n, uint32_t a, uint32_t e)
{ if (a != e) { printf("FAIL %s actual=%lu expected=%lu\n", n, (unsigned long)a, (unsigned long)e); g_fail++; } }

Px4Lite_RemoteMode_t App_GetRemoteDisplayMode(void) { return s_mode; }
Px4Lite_Result_t App_CopyRemoteTelemetry(Px4Lite_RemoteTelemetrySnapshot_t *out, uint32_t now_ms)
{ return Px4Lite_RemoteTelemetryCopySnapshot(out, now_ms); }
/* 其余 App_Copy* / 数据源桩(本测试不触发)。 */
Px4Lite_Result_t App_GetModuleStatus(Px4Lite_ModuleId_t m, Px4Lite_ModuleStatus_t *o) { (void)m; if (o) memset(o,0,sizeof(*o)); return PX4LITE_NOT_READY; }
Px4Lite_Result_t App_CopyEnvironment(App_EnvironmentSnapshot_t *o, uint32_t n) { (void)n; if (o) memset(o,0,sizeof(*o)); return PX4LITE_NOT_READY; }
Px4Lite_Result_t App_CopyAlarm(App_AlarmSnapshot_t *o, uint32_t n) { (void)n; if (o) memset(o,0,sizeof(*o)); return PX4LITE_NOT_READY; }
Px4Lite_Result_t App_CopyNavigation(App_NavigationSnapshot_t *o, uint32_t n) { (void)o;(void)n; return PX4LITE_NOT_READY; }
Px4Lite_Result_t App_CopySystem(App_SystemSnapshot_t *o, uint32_t n) { (void)o;(void)n; return PX4LITE_NOT_READY; }
Px4Lite_Result_t App_CopyMotor(App_MotorSnapshot_t *o, uint32_t n) { (void)o;(void)n; return PX4LITE_NOT_READY; }
Px4Lite_Result_t App_CopyDateTime(App_DateTimeSnapshot_t *o, uint32_t n) { (void)o;(void)n; return PX4LITE_NOT_READY; }
void App_GetCommStats(Px4Lite_CommDebugInfo_t *o) { if (o) memset(o,0,sizeof(*o)); }

int main(void)
{
  Px4Lite_RemoteTelemetrySnapshot_t *snap;
  App_DisplayLogSnapshot_t out;
  uint32_t now = 5000U;

  Px4Lite_RemoteTelemetryInit(now);
  (void)Px4Lite_RemoteTelemetrySetMode(PX4LITE_REMOTE_MODE_REMOTE, now);
  s_mode = PX4LITE_REMOTE_MODE_REMOTE;

  snap = Px4Lite_RemoteTelemetryMutable();
  memset(snap->log_entries, 0, sizeof(snap->log_entries));
  snap->log_entries[0].sequence = 5U; snap->log_entries[0].message_id = 4U; snap->log_entries[0].time_hhmmss = 111U;
  snap->log_entries[1].sequence = 6U; snap->log_entries[1].message_id = 12U; snap->log_entries[1].time_hhmmss = 222U;
  snap->log_count = 2U; snap->log_last_seq = 6U;
  Px4Lite_RemoteTelemetryCommit(2U, 1U, PX4LITE_REMOTE_VALID_LOG, now);

  Eq("remote log ok", App_GetDisplayMessageLog(&out, now), PX4LITE_OK);
  Eq("count", out.count, 2U);
  Eq("e0 mid", out.entries[0].message_id, 4U);
  Eq("e1 seq", out.entries[1].sequence, 6U);
  Eq("version=last_seq", out.version, 6U);

  if (g_fail == 0) { printf("PASS test_app_display_messagelog_remote\n"); return 0; }
  printf("FAIL test_app_display_messagelog_remote fails=%d\n", g_fail); return 1;
}
```

- [ ] **Step 2: 运行确认失败**

```bash
gcc -std=c99 -Wall -Wextra -I Tests/Unit/stubs -I Business/Inc -I Framework/Inc \
  Tests/Unit/test_app_display_messagelog_remote.c Business/Src/app_display_model.c Business/Src/app_message_log.c \
  Framework/Src/px4lite_local_msglog.c Framework/Src/px4lite_remote_telemetry.c -o /tmp/t_rmlog.exe && /tmp/t_rmlog.exe
```
Expected: FAIL —— 现 REMOTE 分支返回 `NOT_READY`。

- [ ] **Step 3: 实现 REMOTE 分支**

把 `Business/Src/app_display_model.c` 的 `App_GetDisplayMessageLog` 改为：
```c
Px4Lite_Result_t App_GetDisplayMessageLog(App_DisplayLogSnapshot_t *out, uint32_t now_ms)
{
  Px4Lite_RemoteTelemetrySnapshot_t remote;
  Px4Lite_Result_t fresh;
  uint16_t i, n;

  if (out == 0) { return PX4LITE_INVALID_PARAM; }

  if (App_GetRemoteDisplayMode() != PX4LITE_REMOTE_MODE_REMOTE) {
    return App_MessageLogCopy(out);
  }

  memset(out, 0, sizeof(*out));
  fresh = AppDisplay_RemoteDomain(&remote, PX4LITE_REMOTE_VALID_LOG, now_ms);
  if (fresh == PX4LITE_NOT_READY) { return PX4LITE_NOT_READY; }

  n = remote.log_count;
  if (n > (uint16_t)APP_DISPLAY_LOG_CAP) { n = (uint16_t)APP_DISPLAY_LOG_CAP; }
  out->version = remote.log_last_seq;
  out->count   = n;
  for (i = 0U; i < n; ++i) {
    out->entries[i].sequence    = remote.log_entries[i].sequence;
    out->entries[i].message_id  = remote.log_entries[i].message_id;
    out->entries[i].time_hhmmss = remote.log_entries[i].time_hhmmss;
    out->entries[i].fault_code  = remote.log_entries[i].fault_code;
    out->entries[i].severity    = remote.log_entries[i].severity;
    out->entries[i].source_id   = remote.log_entries[i].source_id;
    out->entries[i].active      = remote.log_entries[i].active;
  }
  return fresh;
}
```

> `AppDisplay_RemoteDomain` 为 app_display_model.c 既有静态助手(取远端快照 + 按 valid/stale 判新鲜度，见 `App_GetDisplayAlarm`)。若其签名不同，按文件内既有调用一致使用。

- [ ] **Step 4: 运行确认通过 + 回归 + 提交**

```bash
# 本 Task
gcc ... Tests/Unit/test_app_display_messagelog_remote.c ... && /tmp/t_rmlog.exe   # PASS
# 回归 LOCAL
gcc -std=c99 -Wall -Wextra -I Tests/Unit/stubs -I Business/Inc -I Framework/Inc \
  Tests/Unit/test_app_display_messagelog_local.c Business/Src/app_display_model.c Business/Src/app_message_log.c \
  Framework/Src/px4lite_local_msglog.c -o /tmp/t3.exe && /tmp/t3.exe
```
两者 PASS。

```bash
git add Business/Src/app_display_model.c Tests/Unit/test_app_display_messagelog_remote.c
git commit -m "feat: REMOTE模式显示消息日志接口返回远端同步日志"
```

---

### Task 6: 集成验证、工程接入与回归

**Files:** `.eide/eide.yml`；跑全部相关桌面单测 + Keil 构建。

- [ ] **Step 1: 跑齐桌面单测**

```bash
# 全部应 PASS
test_local_msglog / test_remote_tunnel_log / test_mavlink_rx_log_tunnel /
test_app_message_log / test_app_display_messagelog_local / test_app_display_messagelog_remote /
test_remote_tunnel_alarm / test_mavlink_rx_alarm_tunnel / test_app_display_alarm_remote / test_app_display_model_remote_power
```
（链接 `app_message_log.c` 或 `app_display_model.c` 的命令均须含 `Framework/Src/px4lite_local_msglog.c`。）

- [ ] **Step 2: 工程接入**

在 `.eide/eide.yml` 的 Framework 组(`px4lite_remote_tunnel.c` 旁)加：
```yaml
        - path: Framework/Src/px4lite_local_msglog.c
```

```bash
git add .eide/eide.yml
git commit -m "build: 本机消息日志缓冲源文件接入Keil工程"
```

- [ ] **Step 3: Keil 整机构建(用户侧)**

`Rebuild All` 目标 `0 Error(s), 0 Warning(s)`。

- [ ] **Step 4: 实机联调(用户侧)**

REMOTE 模式下，目标设备产生状态变化日志，确认显示端消息日志页按 `sequence` 增量出现、顺序与目标一致；制造丢帧确认容忍空洞；断链确认保留旧日志并标 stale；LOCAL 端日志行为与 2a 一致。

---

## Self-Review

- **Spec coverage（LY/20 Part 2 §12–§15）**：日志载荷布局(§12)→Task2；增量+心跳发送(§13)→Task4；RemoteTelemetry 存储+RX 去重(§14)→Task3；显示层 REMOTE 取数(§15)→Task5。生产者下沉(分层前置)→Task1。
- **分层合规**：日志缓冲与 TUNNEL 编解码全在 Framework；Business `app_message_log`/`app_display_model` 向下调用 Framework；Framework 不含 Business 头。满足 Global Constraints。
- **Placeholder scan**：新建模块/编解码/RX/TX 均完整代码；Task4 Step5、Task5 Step3 标注的真实签名核对点(非占位逻辑)。Task5 Step4 第一条命令省略号仅为重复 Step2 命令，执行时照搬 Step2。
- **Type consistency**：`Px4Lite_LogEntry_t`(Task1)被 Task2 编解码、Task3 RemoteTelemetry、Task4 TX 一致使用；`Px4Lite_LocalMsgLogDrainSince/Copy`(Task1)被 Task4/Task1.5 调用；`PX4LITE_TUNNEL_PT_MESSAGE_LOG`/`PX4LITE_REMOTE_VALID_LOG`/`PX4LITE_REMOTE_LOG_CAP` 全程统一；`App_MessageLogCopy` 委托 Framework(Task1)、被 LOCAL getter(2a)与回归复用。
- **去重/空洞/心跳**：RX 仅 `seq > log_last_seq` 追加(Task3)，TX 游标 `s_last_sent_log_seq` 增量(Task4)，count=0 心跳两端均不改数据，单测覆盖。
- **2a 桥接复用**：display.c 桥接已读 `App_GetDisplayMessageLog`(模式解析)，Task5 接通 REMOTE 后无需再改显示层。
```
