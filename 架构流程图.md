# STM32F407 Formal Sensor Framework — 架构流程图

> 版本: Formal_Framework11 | 2026-06-15 | 9运行任务 · 8种MAVLink消息 · 热插拔恢复 · SD CSV 日志(注册式)

---

## 一、系统总体架��图

```mermaid
flowchart TB
    subgraph HW["🔌 硬件平台 (STM32F407ZGT6 168MHz · Cortex-M4 · 128KB SRAM)"]
        direction LR
        GNSS_HW["ATGM336H<br/>GPS+BDS<br/>USART2 9600"]
        IMU_HW["MPU6050<br/>6轴IMU<br/>I2C1 0x68"]
        BARO_HW["BME280<br/>温/湿/气压<br/>I2C1 0x76"]
        POWER_HW["分压采样<br/>ADC1 CH5<br/>4:1分压"]
        LORA_HW["E22-400T30D<br/>LoRa 数传<br/>USART3 9600"]
        LCD_HW["ATK-MD0700<br/>800×480 LCD<br/>FSMC 8080"]
    end

    subgraph BSP["📡 BSP 板级支持层"]
        direction LR
        BSP1["USART2<br/>DMA1_S5 环形512B<br/>IDLE中断 · 丢最旧"]
        BSP2["I2C1 PB6/PB7 100kHz<br/>9时钟总线恢复<br/>共享MPU6050+BME280"]
        BSP3["USART3<br/>DMA1_S1 RX 512B<br/>DMA1_S3 TX 280B<br/>TC回调=发送确认"]
        BSP4["ADC1 CH5<br/>FSMC Bank1<br/>8080 16-bit"]
    end

    subgraph DRIVER["🔧 传感器驱动层 · 统一接口"]
        direction LR
        D1["GNSS Driver<br/>NMEA状态机<br/>GGA/RMC/GSA/GSV<br/>校验失败丢整帧"]
        D2["IMU Driver<br/>全零拒绝+尖刺检测<br/>自适应自动恢复<br/>stage/bsp诊断"]
        D3["Baro Driver<br/>强制模式<br/>校准补偿"]
        D4["Power Driver<br/>ADC→电压<br/>百分比+低电"]
        D5["LoRa E22<br/>MAVLink解析<br/>非阻塞TX状态机"]
    end

    subgraph ADAPTER["🔄 Platform Adapter"]
        AD["Driver类型→Framework类型<br/>单位换算 · 字段映射<br/>序列号去重 · Result映射<br/>IMU稳定验证门≥3帧"]
    end

    subgraph FRAMEWORK["🧩 PX4-Lite 中间件框架"]
        subgraph TOPICS["Topic 系统"]
            TOP["GNSS Snapshot · IMU FIFO(32深)<br/>Baro Snapshot · Battery Snapshot<br/>Navigation Snapshot · Health Snapshot<br/>Alarm Snapshot · AlarmEvent Queue<br/>全部临界区保护复制"]
        end

        subgraph TASKS["运行时任务"]
            direction LR
            TK1["sensor<br/>10ms idle+4<br/>唯一采集所有者<br/>GNSS/IMU/Baro/Batt"]
            TK2["estimator<br/>20ms idle+3<br/>GNSS→Navigation<br/>IMU→Madgwick"]
            TK3["health<br/>100ms idle+2<br/>超时→OFFLINE<br/>健康快照+喂狗"]
            TK4["comm<br/>10ms idle+2<br/>LoRa+MAVLink 8消息<br/>空口流控"]
            TK5["storage<br/>50ms idle<br/>注册模块·读Framework主题<br/>SD CSV 1Hz"]
        end

        REG["注册表: 多模块独立生命周期(REG→INIT→CHECK→START→RUN) · Storage 同套注册<br/>通用恢复引擎: 遍历已注册模块(含 Storage recover 置标志) · 2s退避 · 无限重试 · 热插拔容错"]
        ALARM["告警系统: 14模块状态→活动表 · 边沿检测 · 最高仲裁 · STATUSTEXT推送"]
    end

    subgraph BUSINESS["💼 Business 业务层"]
        API["app_data_api.h ← 唯��数据读边界<br/>App_CopyNavigation · App_CopySystem<br/>App_CopyEnvironment · App_CopyAlarm"]
        BIZ["biz_system 1s · biz_acq 200ms · biz_display 10ms<br/>EventBus 8主题×8深度 · 非阻塞扇出"]
    end

    subgraph MAVLINK["📡 MAVLink v2 空中遥测"]
        direction LR
        M1["HEARTBEAT 0<br/>1Hz"]
        M2["SYS_STATUS 1<br/>1Hz"]
        M3["ATTITUDE 30<br/>20Hz"]
        M4["SCALED_PRESSURE 29<br/>1Hz"]
        M5["BATTERY_STATUS 147<br/>1Hz"]
        M6["GNSS_DETAIL 147*<br/>1Hz"]
        M7["STATUSTEXT 253<br/>按需"]
    end

    subgraph DEBUG["🔍 独立诊断体系"]
        DBG["DebugTask 100ms idle+1<br/>最低优先级 · 不参与看门狗<br/>8个独立模块开关<br/>IMU: stage/bsp/reinit三字段<br/>LoRa: 千分率丢包统计"]
    end

    subgraph STORAGE["💾 Storage SD 日志(注册模块)"]
        STO["storage task 50ms idle<br/>RegistryStart→WorkRun<br/>读 CopyNav/Baro/Battery→CSV<br/>8深队列·丢最旧·recover置标志"]
        STOSD["bsp_spi SPI3 轮询 · FatFs(_FS_TINY)<br/>SENSOR.CSV/ERROR.CSV · 5s f_sync<br/>PB3/4/5+PA15 · 不入看门狗"]
        STO --> STOSD
    end

    HW --> BSP
    BSP --> DRIVER
    DRIVER --> ADAPTER
    ADAPTER --> TOPICS
    TOPICS <--> TASKS
    TASKS <--> REG
    TASKS --> ALARM
    ALARM --> M7
    TOPICS --> API
    API --> BIZ
    TASKS --> MAVLINK
    TASKS -.-> DBG
    TOPICS --> STO
```

---

## 二、GNSS 全链路数据流（最完整闭环）

```mermaid
flowchart TD
    A["ATGM336H<br/>USART2 9600bps"] -->|"DMA1_S5 环形512B"| B["bsp_gnss.c<br/>IDLE中断推进读位置<br/>溢出: 丢最旧+drop_count"]
    B -->|"BSP_GNSS_GetRxData()"| C["sensor_gnss.c<br/>NMEA状态机: $→体→*校验→\\r\\n<br/>校验失败: 整帧丢弃"]
    C -->|"Gnss_Snapshot_t"| D["Platform Adapter<br/>序列号去重 · 单位换算"]
    D -->|"Px4Lite_SensorGnss_t"| E["PublishGnss()<br/>临界区: ENTER→复制→EXIT"]
    E --> F["GNSS Snapshot Topic"]

    G["MPU6050 I2C1"] -->|"14B寄存器"| H["sensor_mpu6050.c<br/>全零拒绝+尖刺检测<br/>自适应恢复"]
    H -->|"Mpu6050_Snapshot"| I["Platform Adapter<br/>稳定验证门≥3帧"]
    I -->|"Px4Lite_SensorImu_t"| J["PushImu() → IMU FIFO 32深"]

    F --> K["estimator task 20ms<br/>CopyGnss → IsFresh ≤1500ms<br/>GNSS→Navigation映射"]
    J -->|"PopImu ×8"| L["IsFresh ≤200ms"]
    L --> M["Madgwick AHRS<br/>roll/pitch/yaw"]
    K --> N["PublishNavigation()<br/>valid_mask+quality"]
    M --> N

    N --> O1["biz_acq 200ms<br/>App_CopyNavigation<br/>→EventBus扇出"]
    N --> O2["comm task<br/>MAVLink TX<br/>ATTITUDE+GPS_RAW"]
    N --> O3["biz_display 10ms<br/>HMI页面渲染"]

    F -.-> P["health task 100ms<br/>超时→OFFLINE判定<br/>Health快照"]
    J -.-> P
```

---

## 三、IMU 热插拔自适应恢复（FF11 核心优化）

```mermaid
flowchart TD
    N1["Service() → ReadReg(0x3B,14B) → OK"] --> N2["全零拒绝? NO"]
    N2 --> N3["尖刺检测? NO"]
    N3 --> N4["✅ 提交快照<br/>s_read_fail_count=0<br/>stage=NONE"]

    F1["Service() → ReadReg → IO_ERROR"] --> F2["s_read_fail_count++"]
    F2 --> F3["stage=DATA_READ"]
    F3 --> R1{"read_fail_count ≥ 5?"}

    R1 -->|"是"| R2["NoteSampleFailure()<br/>计划100ms后Init<br/>s_accept_next=false"]
    R2 --> R3["TryReinit()<br/>now > s_next_init?"]
    R1 -->|"否"| F1

    R3 -->|"是"| R4["Sensor_MPU6050_Init()"]
    R4 --> R5["Probe → OK? stage=PROBE"]
    R5 -->|OK| R6["WHO_AM_I → OK? stage=WHO_AM_I"]
    R5 -->|FAIL| R10["自适应退避<br/>前5次:100ms<br/>之后:500ms"]
    R6 -->|OK| R7["Configure → OK? stage=CONFIG"]
    R6 -->|FAIL| R10
    R7 -->|OK| R8["✅ s_initialized=1<br/>reinit_count++<br/>清零fail_count"]
    R7 -->|FAIL| R10
    R10 --> R3
    R3 -->|"否(未到时间)"| F1

    R8 --> S1["ImuRead → CopySnapshot OK?"]
    S1 -->|OK| S2["reinit_count变化?"]
    S2 -->|"是(新恢复)"| S3["重置 stable_valid_count=0"]
    S3 --> S4["stable_valid_count++"]
    S2 -->|"否"| S4
    S4 --> S5{"≥3帧?"}
    S5 -->|"是"| S6["✅ 发布到Framework"]
    S5 -->|"否"| S7["❌ 拦截丢弃"]
    S1 -->|FAIL| S7

    N4 --> F1
    S6 --> N1
    S7 --> S1
```

---

## 四、MAVLink 收发链路

```mermaid
flowchart LR
    subgraph TX["📤 LoRa TX (comm task 10ms)"]
        T1["MavlinkTxRun()"] --> T2{时隙到期?}
        T2 -->|"HEARTBEAT 1s"| T3["心跳: type=四旋翼 STANDBY"]
        T2 -->|"SYS_STATUS 1s"| T4["SNS在位/健康<br/>电池12.4V"]
        T2 -->|"ATTITUDE 50ms"| T5["roll/pitch/yaw + 角速率"]
        T2 -->|"SCALED_PRESSURE 1s"| T6["1002hPa · 27.9°C"]
        T2 -->|"BATTERY_STATUS 1s"| T7["电压mV · 百分比 · 低电"]
        T2 -->|"GNSS_DETAIL 1s"| T8["自定义 lat/lon/alt/sats"]
        T2 -->|"STATUSTEXT 2s"| T9["PX4LITE ALARM GNSS 0x0202"]
        
        T3 & T4 & T5 & T6 & T7 & T8 & T9 --> T10["Lora_E22_Send()<br/>异步复制语义"]
        T10 --> T11["TX状态机<br/>IDLE→WAIT_AUX→SENDING→IDLE<br/>AUX超时200ms · DMA超时500ms"]
        T11 --> T12["BSP_LoRa_StartSend()<br/>复制到s_tx_buf<br/>HAL_UART_Transmit_DMA"]
    end

    subgraph AIR["📡 空中 9600bps"]
        AIR1["MAVLink v2 帧<br/>8种消息<br/>CRC-16校验"]
    end

    subgraph RX["📥 LoRa RX (comm task 10ms)"]
        RX1["USART3 DMA1_S1 环形512B<br/>IDLE ISR推进读位置"]
        RX1 --> RX2["BSP_LoRa_GetRxData()"]
        RX2 --> RX3["mavlink_parse_char()<br/>逐字节组装"]
        RX3 -->|"完整帧"| RX4["Lora_RxFrame_t<br/>临界区复制"]
        RX4 --> RX5["统计: rx_frame_count<br/>crc/parse/drop/overflow"]
    end

    subgraph STAT["📊 链路统计"]
        ST1["空中丢包: 0% (CRC全通过)<br/>TX busy流控: 94.3%<br/>1Hz消息零丢失<br/>ATTITUDE通过率~11%"]
    end

    T12 --> AIR1 --> RX1
    RX5 --> ST1
```

---

## 五、启动与看门狗门控

```mermaid
flowchart TD
    I1["HAL_Init()"] --> I2["SystemClock_Config()<br/>HSE→PLL 168MHz"]
    I2 --> I3["DebugConsole_Init()<br/>USART1 115200"]
    I3 --> I4["BSP_Init()<br/>GPIO+DMA+UART+I2C+ADC+FSMC"]
    I4 --> I5["Px4Lite_PlatformInit()"]
    I5 --> I6["Px4Lite_AppInit()<br/>TopciInit · ModulesInit · RegistryInit<br/>Register: GNSS+IMU+Baro+Batt+Est+LoRa<br/>创建: sensor+estimator+health+comm"]
    I6 --> I7["Business_AppInit()<br/>EventBusInit<br/>创建: biz_system+biz_acq+biz_display"]
    I7 --> I8["DBG_BOOT_PRINT"]
    I8 --> I9["vTaskStartScheduler()"]

    subgraph WATCHDOG["🛡️ 看门狗门控 (health task 100ms)"]
        W1["各任务周期结束:<br/>PlatformHeartbeat(ID,now_ms)"] --> W2["heartbeat_ms[id]=now_ms<br/>heartbeat_seen_mask"]
        W2 --> W3["PlatformWatchdogFeed()"]
        W3 --> W4["HeartbeatsHealthy()?"]
        W4 -->|"需要: SENSOR+ESTIMATOR+HEALTH<br/>+SYSTEM+BUSINESS+DISPLAY<br/>全出现且全部 <500ms"| W5["✅ BSP_WatchdogRefresh()"]
        W4 -->|"任一缺位或超时"| W6["❌ 不喂 → 系统复位"]
    end
```

---

## 六、模块生命周期

```mermaid
flowchart LR
    L1["REGISTERED"] --> L2["INITIALIZING<br/>init回调"]
    L2 -->|"OK"| L3["SELF_CHECK<br/>self_check回调"]
    L3 -->|"OK"| L4["STARTING<br/>start回调"]
    L4 -->|"OK"| L5["RUNNING ✅"]
    
    L2 -->|"失败"| L6["FAILED ❌"]
    L3 -->|"失败"| L6
    L4 -->|"失败"| L6
    
    L6 -->|"RecoveryMonitor<br/>2s退避 · 无限重试<br/>recover回调"| L7["RECOVERING"]
    L7 -->|"OK→回到REGISTERED<br/>重新走Start流程"| L1
```

---

## 七、整体架构优势

```
┌─────────────────────────────────────────────────────────────────┐
│  ① PX4架构理念适配MCU                                             │
│  uORB Topic → Snapshot/FIFO静态数组 · Module Lifecycle → 独立注册表 │
│  Health & Fault → 超时判定+告警 · Recovery → 通用遍历恢复引擎      │
├─────────────────────────────────────────────────────────────────┤
│  ② 5层单向依赖解耦    Core→Business→Framework→Adapter→Driver→BSP  │
│  每层职责边界明确 · 新传感器只需加描述符即可接入                      │
├─────────────────────────────────────────────────────────────────┤
│  ③ 数据完整性三重保障                                              │
│  防半包(NMEA状态机) · 防撕裂(临界区复制) · 发送确认(TC回调)         │
│  IMU: 全零拒绝+尖刺检测+稳定验证门                                  │
├─────────────────────────────────────────────────────────────────┤
│  ④ 热插拔容错                                                      │
│  通用恢复引擎+IMU自适应退避 · recover回调设标志位(不碰总线)          │
│  恢复后稳定验证门 拦截起振垃圾数据                                   │
├─────────────────────────────────────────────────────────────────┤
│  ⑤ MAVLink v2 8消息全面遥测                                       │
│  HEARTBEAT · SYS_STATUS · ATTITUDE · SCALED_PRESSURE              │
│  BATTERY_STATUS · GNSS_DETAIL · STATUSTEXT · 地面站原生支持         │
├─────────────────────────────────────────────────────────────────┤
│  ⑥ 独立诊断 · 运行稳定                                             │
│  8模块独立Debug · DebugTask不参与喂狗 · 堆62% · 栈全部>25%         │
│  ARMCC5 0 Error / 0 Warning                                       │
└─────────────────────────────────────────────────────────────────┘
```
